# [문서 A] · 지구15 계산최적화·메타휴리스틱 · 다루는 항목: 시뮬레이티드어닐링~크릴로프부분공간 10개
# ← 직전: "거대한 최적화를 어떻게 쪼개고 가속?"(파일 17)  |  → 다음: "수천 입자 얽힘을 어떻게 압축해 다루나?"(파일 19, 지구16)
# 이 파일 내부 진행:
[x] 어닐링 [x] PSO [x] 비선형최소제곱 [x] 레벤버그마카트 [x] 신뢰영역 [x] ADMM [x] 자동미분 [x] 행렬지수함수 [x] 체비쇼프근사 [x] 크릴로프부분공간

---

## 이 지구를 여는 핵심 의문

> **"거대한 최적화를 어떻게 '쪼개고 가속'하나?"**

최적화(optimization)란 "가장 좋은 답을 찾는 일"이다 — 골짜기의 가장 낮은 바닥, 또는 산의 가장 높은 봉우리를 찾는 사냥이다(지구07에서 변분법·경사하강의 기본 연장을 벼렸다). 그런데 진짜 문제는 변수가 100만 개씩 되고, 골짜기가 울퉁불퉁 수천 개씩 파여 있고, 함수 한 번 계산하는 데도 비싸다. 이런 *거대한* 최적화 앞에서 "산을 그냥 아래로 굴러내려가는" 단순한 방법은 가짜 골짜기(국소최소, local minimum)에 처박히거나, 너무 느려서 끝나지 않는다. 우리는 이 지구에서 거대한 최적화를 **두 갈래로 공략하는** 연장을 차례로 벼린다 — (가) 문제를 **작은 조각으로 쪼개** 나눠 푸는 연장, (나) 계산을 **가속해** 적은 횟수로 답에 닿는 연장.

**닿는 난제:** #16(거대 최적화·조합폭발 — 변수가 천문학적으로 많은 문제), #19(다물체·다파라미터 추정 — 수많은 관측을 한 모델에 맞춤), #22(비선형 동적평형 — 흔들리는 시스템을 균형으로 모는 제어).

**못박을 함정 (이 지구 내내 유효):** **"빠르다고 항상 정답에 가까워지진 않는다."** 메타휴리스틱(metaheuristic)이나 가속 기법은 "빨리 그럴듯한 답을 준다"는 약속을 한다. 하지만 *빠른 수렴(convergence)*과 *옳은 답으로의 수렴*은 다른 말이다. 어떤 가속법은 엉뚱한 점으로 빠르게 달려가 버린다. 어떤 휴리스틱은 운이 나쁘면 영원히 가짜 골짜기에 갇힌다. 이 지구의 모든 카드에서 "이건 *언제* 진짜 답을 보장하나, 언제 그냥 빠르기만 한가"를 매번 못박는다.

**중의성 못박기 (이 지구 내내 유효):**
- **λ(람다)**: 이 지구에서 λ는 세 뜻으로 나뉘어 나온다 — (가) 레벤버그-마카트·신뢰영역에서 **감쇠 인자(damping factor)** = "한 걸음을 얼마나 조심스럽게 줄일까"의 손잡이, (나) ADMM에서 **라그랑주 승수(Lagrange multiplier)** = "약속(제약)을 어긴 벌금의 가격표", (다) 행렬지수·크릴로프에서 **고유값(eigenvalue)** = "그 방향에서 늘어나는 배율"(지구01에서 벼렸다). 등장할 때마다 어느 쪽인지 다시 못박겠다.
- **∇(나블라, del)**: "그래디언트(gradient) 기호". 함수가 *어느 방향으로 가장 가파르게 오르는가*를 가리키는 화살표다. ∇f를 "f의 오르막 화살표 묶음"이라 읽어라. 첫 카드에서 정식 해부한다.
- **J**: 이 지구에서 J는 두 뜻이다 — (가) 비선형최소제곱·레벤버그-마카트에서 **자코비안(Jacobian)** = "여러 출력이 여러 입력에 어떻게 반응하나를 적은 미분 표", (나) 가끔 비용함수(cost) 자체. 매번 못박는다. 비용함수 쪽은 가능하면 다른 글자를 쓴다.

직전 파일 17(지구14, 고등함수해석·특수함수)은 마지막에 "특수함수와 적분변환으로 식은 풀었는데, 변수가 100만 개인 *거대한 최적화*는 어떻게 쪼개고 가속하나?"라는 의문을 남겼다. 그 의문을 첫 도구 **시뮬레이티드 어닐링**의 ①에서 받는다.

> **이 지구 뿌리의 출처:** 이 지구의 모든 갈래는 한 뿌리 — "거대한 최적화는 *쪼개기*와 *가속하기* 두 갈래로만 공략한다" — 에서 자란다. 그 뿌리에서 탐색을 흩는 줄기(어닐링·PSO)와 문제를 나누는 줄기(ADMM), 한 걸음을 똑똑히 정하는 줄기(비선형최소제곱·레벤버그-마카트·신뢰영역·자동미분), 거대 계산을 압축·가속하는 줄기(행렬지수·체비쇼프·크릴로프)가 사방으로 뻗는다. 매 갈래마다 못박는 물: "빠르다고 항상 정답에 가까워지진 않는다."

---

## ■ 시뮬레이티드 어닐링 / Simulated Annealing   지구15 · 선행: 지구07 경사하강, 지구04 확률 → 잠금해제: #16

### ① 의문
(파일 17이 남긴 "거대한 최적화를 어떻게 쪼개고 가속하나"를 받는다.) 첫 질문은 이거다. **골짜기가 수천 개로 울퉁불퉁 파인 땅에서, 가짜 골짜기(국소최소)에 처박히지 않고 진짜 가장 깊은 바닥을 어떻게 찾나?** 그냥 아래로만 굴러내려가면 처음 만난 작은 웅덩이에 갇혀버린다. 어떻게 하면 웅덩이를 *빠져나와* 더 깊은 곳을 계속 찾아갈까?

### ② 벽
경사하강(gradient descent)은 "항상 더 낮은 쪽으로만" 한 걸음씩 내려간다(지구07). 이 "항상 낮은 쪽으로만"이라는 규칙이 바로 벽이다 — 작은 웅덩이에 떨어지면, 거기서 나가려면 *잠깐 위로* 올라가야 하는데, 규칙이 그걸 금지한다. 그래서 가짜 바닥에서 영원히 멈춘다.

```
   비용(높이)
    │   ╲          ╱╲          ╱
    │    ╲   ╲    ╱  ╲   ╲    ╱      ← 공이 여기 작은 웅덩이에 빠지면
    │     ╲_╱ ●  ╱    ╲_╱  ╲_╱           "아래로만" 규칙 때문에 못 나옴
    │      가짜  ╱      ╲                 (진짜 바닥은 저 아래인데)
    │      바닥 ╱        ╲___ ← 진짜 가장 깊은 바닥
    └──────────────────────────▶ 위치
```

### ③ 발명
쇠를 달궜다 천천히 식히면(어닐링, annealing) 원자들이 가장 안정된(에너지 가장 낮은) 자리로 스스로 정렬된다 — 이 물리를 흉내 낸다. 핵심 발명: **"가끔 일부러 위로도 올라가도록 허락하되, 그 허락 확률을 '온도'로 조절하고, 온도를 천천히 낮춘다."** 뜨거울 땐 마구 돌아다녀 웅덩이를 넘고, 식을수록 점점 아래로만 가라앉아 결국 진짜 바닥에 정착한다. 새 기호: 온도 **T**, 받아들임 확률 **P = exp(−ΔE / T)**.

### ④ 해부

**R0 — 중2의 눈.**

   [중2 시각] 어닐링 — 온도를 내리며 가짜 골짜기를 넘어 진짜 바닥에 정착
```
   뜨거움(T 큼)        식는 중(T 중간)        차가움(T≈0)
   막 흔들림            덜 흔들림              거의 안 흔들림
     ●↗ ↘ ●↗            ●  ↗                    .
   ╲_╱╲_╱╲___╱        ╲_╱╲_╲___╱            ╲_╱╲_╱╲___╱
    웅덩이 펄쩍 넘음      큰 골짜기 안에만        ●  진짜 바닥에
                                              가만히 정착
   T: ████████  →  ████  →  █  →  ▁  (온도가 천천히 내려간다)
```
   처음엔 뜨겁게 마구 흔들어 작은 웅덩이를 펄쩍 넘고, 온도를 천천히 내리면 큰 골짜기 안에 갇혔다가, 마지막엔 가장 깊은 바닥에 가만히 정착한다.
   Start hot to jump over fake valleys, cool slowly to settle into the deepest true bottom.

산골짜기에 구슬을 놓고 찾는다 치자. 처음엔 골짜기 전체를 마구 흔든다(뜨거움) — 구슬이 작은 웅덩이를 펄쩍 넘는다. 흔들기를 *조금씩 줄이면*(식힘) 구슬은 점점 큰 골짜기 안에만 머물고, 마지막엔 그 안 가장 깊은 바닥에 가만히 멈춘다. "처음엔 막 흔들고 점점 살살" — 이게 어닐링의 전부다.

▲ **R1로 — 이유:** "흔드는 세기"와 "위로 올라가는 걸 허락할 확률"을 *숫자와 식*으로 적어야 컴퓨터가 따라 한다.

**R1 — 고교의 손.**

   [중2 시각] 받아들임 확률 P=exp(−ΔE/T) — 온도가 손잡이
```
   P(위로 올라갈 허락)
   1 ┤●▁                    뜨거움(T 큼): 곡선이 완만
     │  ▔▁▁  T 큼            → 손해 ΔE 커도 잘 허락(막 넘음)
     │      ▔▔▁▁
     │ T 작음 ╲  ▔▔▔▁▁▁     차가움(T 작음): 곡선이 가파름
   0 ┤        ╲▁▁▁▁▁▁▁▁     → 조금만 손해 봐도 거절(아래로만)
     └──────────────────→ ΔE(손해 크기)
```
   손해 ΔE가 크거나 온도 T가 낮을수록 위로 올라가는 걸 덜 허락한다. 이 한 식이 "뜨거우면 막 넘고, 식으면 안 넘는다"를 그대로 담는다.
   Bigger loss ΔE or lower temperature T means less chance to climb — the formula encodes "hot jumps, cold settles."

한 걸음: 지금 위치에서 이웃 위치를 아무렇게나 하나 고른다. 그 이웃의 비용 변화 **ΔE = (이웃 비용) − (지금 비용)** 를 본다.
- ΔE < 0 (이웃이 더 낮음): 무조건 간다.
- ΔE > 0 (이웃이 더 높음): **확률 P = exp(−ΔE / T)** 로만 간다.

한 글자씩:
- **ΔE**: "델타 이"라 읽는다. Δ(델타)는 "변화량·차이"를 뜻하는 삼각형 기호. E는 비용(에너지처럼 보고 Energy의 E). ΔE = 옮겨가면 비용이 *얼마나 늘어나나*. 양수면 손해, 음수면 이득.
- **T**: 온도(Temperature). 진짜 온도가 아니라 "지금 얼마나 마구 흔드는가"를 나타내는 손잡이 숫자. 크면 마구 돌아다님, 작으면 거의 아래로만.
- **exp(·)**: "익스포넌셜(지수함수)". exp(x) = eˣ, 여기서 **e**는 약 2.718인 자연상수(자연로그의 밑). exp는 "입력이 커지면 폭발적으로 커지고, 음수면 0에 가깝게 작아지는" 함수. exp(0)=1, exp(−큰수)≈0.
- **P = exp(−ΔE/T)**: 통째 의미 — "손해(ΔE)가 클수록, 그리고 온도 T가 낮을수록, 위로 올라가는 걸 *덜* 허락한다." ΔE/T가 작으면(손해 작거나 온도 높음) exp는 1에 가까워 거의 허락, 크면 exp는 0에 가까워 거의 거절. 이 한 식이 "뜨거우면 막 넘고 식으면 안 넘는다"를 정확히 담는다.

▲ **R2로 — 이유:** "천천히 식히면 *진짜로* 가장 깊은 바닥을 찾는다"가 단순 비유가 아니라 *증명된 정리*인지 따져야 한다.

**R2 — 학부의 머리.**
어닐링은 마르코프 연쇄(Markov chain, 지구13에서 벼렸다)의 일종이고, 받아들임 규칙 P=exp(−ΔE/T)는 **메트로폴리스 규칙(Metropolis criterion)**이다. 정해진 온도 T에서 충분히 오래 돌리면, 방문 확률은 **볼츠만 분포(Boltzmann distribution)** π(x) ∝ exp(−E(x)/T)에 수렴한다 — "비용 낮은 자리에 더 자주 머문다." 핵심 정리: 온도를 **T(k) ≥ c / log(1+k)** (k는 걸음 수, c는 상수, log는 로그)보다 *느리게* 식히면 *확률 1로* 전역최소(가장 깊은 바닥)에 수렴한다. 단 — 이 "log보다 느리게"는 **현실에선 너무 느려서** 우주 나이만큼 걸릴 수 있다. 여기서 이 지구의 함정이 첫 번째로 박힌다: **"수렴 보장은 있지만, 그 보장을 받는 식힘 속도는 실용적이지 않다."** 실제론 더 빨리 식히고 "그�럭저럭 좋은 답"으로 타협한다 — 그 답이 진짜 바닥이라는 보장은 *그 순간 사라진다.*

▲ **R3로 — 이유:** 이게 더 큰 가족(확률적 전역최적화)의 어느 자리에 있는지, 다른 도구와 어떻게 잇는지 봐야 한다.

**R3 — 대학원의 시야.**
어닐링은 "확률로 탐색하는 메타휴리스틱" 가족의 맏이다. 다음 카드 **PSO**(입자군집)는 "여럿이 동시에 흩어져 탐색"으로 같은 벽(가짜 골짜기)을 공략하는 사촌이다. 어닐링의 받아들임 규칙은 통계물리의 몬테카를로(Monte Carlo) 표집과 같은 수학이고, 머신러닝의 시뮬레이티드 어닐링·양자 어닐링(quantum annealing, 터널링으로 벽을 *통과*)으로 뻗는다. #16(조합폭발)의 외판원 문제·반도체 배치 같은 *이산* 최적화에서 특히 쓴다 — 미분이 없어 경사하강을 못 쓰는 땅이기 때문.

▲ **R4로 — 이유:** "언제 빠르게 식혀도 안전한가"의 일반 답이 없는 지점으로 간다.

**R4 — 최전선의 칼날.**
어떤 문제에서 *얼마나 빨리 식혀도 전역최소를 놓치지 않는가*에 대한 일반 이론은 없다 — 문제의 "에너지 지형(landscape)" 모양에 따라 천차만별이고, 그 모양을 미리 아는 게 원래 문제만큼 어렵다. 가짜 골짜기들이 얼마나 깊고 좁은지(스펙트럼 갭, spectral gap)가 식힘 속도를 결정하는데, 실제 문제에서 이 갭을 추정하는 일반법은 — **여기서부터 인류가 모른다.** "빠른 식힘 일정(adaptive cooling)"은 대부분 경험칙이고, 왜 그게 통하는지의 보장은 부분적이다.

**【정밀층 — 집필표준 v2】**
[락] 본 카드에서 T는 온도(흔드는 세기) 손잡이이지 온도·장력·전치가 아니고, ΔE는 비용 변화, Δ는 변화량(신뢰반지름 아님), exp는 지수함수.
R1° 정의역·공역: 어닐링은 사상 (현 상태 x, 이웃 후보 x′, 온도 T) ↦ 받아들임 확률 P = exp(−ΔE/T) ∈ (0,1] (ΔE>0일 때), 그리고 온도 일정 T(k)↘. 상태공간을 확률적으로 떠도는 마르코프 연쇄. 키르크패트릭(1983)이 금속 풀림 물리에서 발명.
R2° 보조정리: Lemma(평형분포가 볼츠만). 고정 T에서 메트로폴리스 규칙은 세부균형 π(x)P(x→x′)=π(x′)P(x′→x)을 π(x)∝exp(−E(x)/T)에서 만족한다. 귀납·검증: P(x→x′)=min(1,exp(−ΔE/T))를 대입하면 양변이 모두 exp(−(E(x)+E(x′)+|ΔE|)/T)에 비례해 같아진다 → 충분히 오래 돌리면 볼츠만 분포로 수렴(비용 낮은 곳에 더 자주).
R3° 구조 편입: 어닐링은 확률적 전역최적화(메타휴리스틱) 가족의 맏이로, 마르코프 연쇄 몬테카를로(MCMC)의 온도 담금질판. 대수↔해석 사상: 받아들임 P=exp(−ΔE/T)가 "에너지 지형의 통계물리 대수"를 "표집·탐색의 해석"으로 잇고, T→0 극한에서 볼츠만이 최저점에 집중.
R4° 파산(수식): 수렴 보장 식힘이 비실용적으로 느려 정답이 도주한다 — 전역최소를 확률 1로 보장하려면 T(k) ≥ c/log(1+k)보다 느리게 식혀야 하는데, 이는 k번째 걸음에 T~1/log k라 ε 정밀도에 e^{c/ε}번(우주 나이) 걸린다. 실용적으로 빨리 식히면 그 순간 전역최소 보장이 증명에서 빠져나가, 스펙트럼 갭(가짜 골짜기 깊이)이 작을 때 국소최소에 갇힘 → "빠름이 정답을 보장하지 않는다."
쉽게: 처음엔 뜨겁게 마구 흔들어 가짜 웅덩이를 펄쩍 넘고 천천히 식혀 진짜 바닥에 앉히는데, 보장받는 식힘 속도는 너무 느려 현실에선 못 쓴다.

### ⑤ 새 의문
어닐링은 *구슬 하나*를 흔들어 식힌다 — 운 나쁘면 좋은 골짜기를 영영 못 만난다. **그럼 구슬을 *여럿* 동시에 뿌려, 서로 정보를 나누며 같이 좋은 골짜기로 모이게 하면 어떨까?** → 다음 도구 **입자군집최적화(PSO)**의 ①로. 닿는 난제: #16.

**[도구 창조 씨앗]** 규칙 "온도를 *낮춘다*"를 "온도를 *올렸다 내렸다 진동시킨다*"로 뒤집으면? → 재가열 어닐링(reheating), 갇히면 다시 데워 탈출. 위험 검문소: **③무모순** — 재가열은 "단조 식힘이면 수렴"이라는 정리의 전제를 깨므로, 수렴 보장이 *증명에서 빠져나간다*. 빨라 보여도 "정답 보장"은 잃는다(함정 재확인).

---

> 〔뿌리 줄기〕 **방금 시뮬레이티드 어닐링으로 "구슬 하나를 흔들며 식혀 가짜 골짜기를 탈출하는 탐색"을 얻었다.** 그런데 구슬이 *하나*뿐이라 운 나쁘면 좋은 골짜기를 영영 못 만난다. 여기서 갈래가 갈라진다.
> ```
> 어닐링(구슬 하나 흔들기)
>  ├─ 줄기①(다음 도구): 구슬을 여럿 뿌려 정보 나누며 함께 모이기 → 입자군집최적화(PSO)
>  ├─ 곁가지: 식힘 일정이 전역최소를 보장 못 함 → 함정 "빠름 ≠ 정답"
>  └─ 곁가지: 조합폭발·핵융합 난류 → 난제 #16
> ```
> "구슬 하나가 외로우면, *여럿이 서로 신호를 주고받게* 하자" — 다음 카드가 그 군집을 벼린다.

---

─────────────  ▶ 문제 트레이닝: 시뮬레이티드 어닐링(Simulated Annealing)  ─────────────

【예제】
 예제 1. 지금 비용 10, 이웃 비용 7. ΔE는? 받아들이나?
   풀이: ΔE = (이웃) − (지금) = 7 − 10 = −3. 음수(이웃이 더 낮음) → 무조건 받아들인다. 이유: ΔE<0이면 확률 계산 없이 항상 간다. ∴ ΔE=−3, 받아들임.
 예제 2. ΔE=2, T=2. 받아들임 확률 P=exp(−ΔE/T)는?
   풀이: P = exp(−2/2) = exp(−1) = 1/e ≈ 0.368. 이유: 손해 걸음(ΔE>0)이라 P로만 받아들인다. ∴ P≈0.368.
 예제 3. 같은 ΔE=2인데 T를 4로 올리면 P는? T=1로 내리면?
   풀이: T=4 → exp(−2/4)=exp(−0.5)≈0.607. T=1 → exp(−2/1)=exp(−2)≈0.135. 이유: 온도가 높을수록(분모 큼) 지수가 0에 가까워 P가 1쪽, 낮을수록 P가 0쪽. ∴ 0.607, 0.135. "뜨거우면 막 넘고 식으면 안 넘는다"가 숫자로 보인다.

【문제】
 ◇기초 1. 지금 비용 5, 이웃 비용 8. ΔE는? 2. ΔE=0, T=3일 때 P=exp(−ΔE/T)는?
 ◇기본 3. ΔE=3, T=3. 받아들임 확률 P는? (exp(−1)≈0.368 사용)
        4. ΔE=−4일 때 받아들이나? 확률 계산이 필요한가?
 ◇유형 5. ΔE=1로 같은데 T=10에서 P와 T=0.5에서 P를 각각 구하고, 어느 온도가 더 잘 넘는지 답하라. (exp(−0.1)≈0.905, exp(−2)≈0.135)
        6. 온도를 T(k)=T₀/(1+k)로 식힌다. T₀=12, k=0,1,3일 때 온도는?
 ◇심화 7. 손해 ΔE=4 걸음을 P≥0.5로 받아들이려면 온도 T가 얼마 이상이어야 하나? (ln2≈0.693)
        8. 두 이웃 후보: A는 ΔE=−1, B는 ΔE=2(T=2). A는 확률 얼마로, B는 확률 얼마로 받아들이나? 더 자주 받아들이는 쪽은?
 ◇최상위 9. 전역최소 보장 식힘 T(k)≥c/log(1+k)를 따른다. k=1에서 log(2)≈0.693, k=9에서 log(10)≈2.303(자연로그). c=1일 때 두 온도의 비 T(1)/T(9)를 구하고, k가 100, 10000으로 커질 때 온도가 "얼마나 천천히" 떨어지는지(0에 닿는 속도) 말로 설명하라. 이 느림이 왜 함정인가?

【해설】
 1. ΔE=(이웃)−(지금)=8−5=3. 공식 직접 대입. 흔한 실수: 순서를 뒤집어 −3으로 쓰기(항상 이웃 빼기 지금). 답: ΔE=3.
 2. P=exp(−0/3)=exp(0)=1. ΔE=0이면 손해가 없어 확률 1로 받아들인다. 답: 1.
 3. P=exp(−3/3)=exp(−1)≈0.368. 손해 걸음이라 P로만. 흔한 실수: 음수 부호 빼먹고 exp(+1)≈2.72(확률이 1 넘음, 불가능). 답: ≈0.368.
 4. ΔE=−4<0(이웃이 더 낮음) → 무조건 받아들임, 확률 계산 불필요. 흔한 실수: 음수도 exp에 넣어 계산하기. 답: 받아들임, 계산 불필요.
 5. T=10: P=exp(−1/10)=exp(−0.1)≈0.905. T=0.5: P=exp(−1/0.5)=exp(−2)≈0.135. 분모(온도)가 클수록 지수가 0에 가까워 P가 큼. 답: 0.905, 0.135 — 뜨거운 T=10이 훨씬 잘 넘는다.
 6. T(0)=12/1=12, T(1)=12/2=6, T(3)=12/4=3. 식힘 일정 직접 대입. 답: 12, 6, 3.
 7. P=exp(−4/T)≥0.5. 양변 ln: −4/T ≥ ln0.5 = −0.693. 부등호: −4/T ≥ −0.693 → 4/T ≤ 0.693 → T ≥ 4/0.693 ≈ 5.77. 흔한 실수: ln 취할 때 부등호 방향(음수로 곱하니 뒤집힘) 놓치기. 답: T ≥ 약 5.77.
 8. A: ΔE=−1<0 → 확률 1(무조건). B: ΔE=2, T=2 → P=exp(−2/2)=exp(−1)≈0.368. 더 자주 받아들이는 쪽은 A(확률 1). 답: A는 1, B는 ≈0.368, A가 더 자주.
 9. T(1)=1/0.693≈1.443, T(9)=1/2.303≈0.434. 비 T(1)/T(9)=0.434 분의 1.443 ≈ 3.32(또는 log10/log2≈3.32). k=100이면 log(101)≈4.6, k=10000이면 log(10001)≈9.2 — k가 100배 커져도 온도는 겨우 약 2배만 떨어진다(log은 아주 천천히 자란다). 함정: 전역최소를 보장받으려면 이 "log만큼 느린" 식힘을 지켜야 하는데, 그러면 ε 정밀도에 닿는 데 걸음 수가 천문학적(우주 나이급)이라 *현실에서 못 쓴다*. 빨리 식히면 그 순간 보장이 증명에서 빠져나간다. 답: 비 ≈3.32, log 식힘은 극도로 느려 실용 불가 — "보장은 있으나 실용적이지 않다"가 함정.

【정답】 1) ΔE=3  2) 1  3) ≈0.368  4) 받아들임(계산 불필요)  5) 0.905와 0.135, T=10이 잘 넘음  6) 12,6,3  7) T≥약5.77  8) A=1, B≈0.368, A가 더 자주  9) 비≈3.32, log 식힘이 너무 느려 실용 불가(함정)
【발명의 씨앗】 기초~유형까지는 P=exp(−ΔE/T) 한 식에 숫자만 넣으면 깔끔히 풀렸다 — 온도가 높으면 잘 넘고 낮으면 안 넘는다가 또렷했다. 심화 7에서 "받아들임 확률을 정하려면 온도를 얼마로?"라는 *역방향* 질문이 슬슬 까다로워졌고, 최상위 9에서 벽이 터졌다: *전역최소를 보장하는* 식힘 속도 T(k)≥c/log(1+k)가 너무 느려 우주 나이만큼 걸린다. 이 공식은 "충분히 천천히 식히면 완벽"한데 "실용적으로 빨리 식히면" 보장이 □무너진다. 그 무너짐이 폭발하는 곳 = 난제 #16(거대 최적화·조합폭발). 이 난제용 도구란, "빠르게 식혀도 전역최소를 놓치지 않는" 이 불편함을 메우는 것.

---

## ▣ 입자군집최적화 / Particle Swarm Optimization (PSO)   지구15 · 선행: ← 어닐링, 지구08 동역학 → 잠금해제: #16

### ① 의문
(어닐링 ⑤에서 받음.) 구슬 하나로는 운이 좌우한다. **새 떼나 물고기 떼처럼, 여러 탐색자가 *동시에* 흩어져 돌아다니며 "내가 본 가장 좋은 곳"과 "우리 떼가 본 가장 좋은 곳"을 서로 공유해 같이 좋은 골짜기로 모일 수 없을까?** 혼자보다 떼가 똑똑하지 않을까?

### ② 벽
어닐링은 한 점만 움직인다. 그 점이 우연히 나쁜 영역에서 출발하면, 좋은 골짜기의 존재 자체를 *모른다* — 정보가 한 곳에만 있어서 공유가 없다. 또 미분(∇)을 안 쓰는 건 장점이지만, "어디로 가야 빠른가"라는 방향 정보도 버려서 느리다.

```
   탐색공간(위에서 본 지도, ●=입자, ★=좋은 골짜기)
   ┌─────────────────────────┐
   │  ●        ●         ★   │   여러 입자가 흩어져 있고
   │      ●        ●         │   각자 "내 최고기록"과
   │   ●      ★보다 나쁨     │   "떼 전체 최고기록 ★"을 향해
   │        ●           ●    │   조금씩 끌려간다
   └─────────────────────────┘
```

### ③ 발명
탐색자 여럿(입자, particle)을 공간에 뿌린다. 각 입자는 위치 **x**와 속도 **v**를 가진다. 매 걸음, 속도를 세 힘으로 갱신한다 — (가) 관성(지금 가던 방향 유지), (나) **개인 최고(pbest)**로 끌림, (다) **떼 전체 최고(gbest)**로 끌림. 새 기호: 입자 i의 속도 갱신식 **vᵢ ← w·vᵢ + c₁·r₁·(pᵢ − xᵢ) + c₂·r₂·(g − xᵢ)**.

### ④ 해부

**R0 — 중2의 눈.**

   [중2 시각] PSO — 흩어진 새 떼가 명당(★)으로 함께 모임
```
   처음(흩어짐)            중간(끌림)            끝(모임)
   ●   ●      ★           ●  ●  ★              ●●★
      ●   ●                  ● ●↗                 ●
   ●     ●                ●  ↗●                  ●
   각자 제멋대로            내 최고+떼 최고          ★ 둘레로
                          쪽으로 휨                 다 모임
```
   각 새는 "내가 본 명당"과 "떼가 본 명당 ★" 두 쪽으로 동시에 끌린다. 둘을 섞어 날면 떼 전체가 가장 좋은 자리로 모인다.
   Each bird is pulled toward its own best and the swarm's best ★; blending both, the flock gathers at the top spot.

새 떼가 먹이를 찾는다. 각 새는 "내가 지금껏 본 가장 좋은 자리"를 기억하고, 동시에 "우리 떼가 본 가장 좋은 자리"를 듣는다. 그래서 자기 기억 쪽으로도 끌리고, 떼 전체가 찾은 명당 쪽으로도 끌린다. 둘을 적당히 섞어 날면, 떼 전체가 점점 가장 좋은 자리로 모인다.

▲ **R1로 — 이유:** "끌린다"를 *숫자로 더하는 식*으로 적어야 한다.

**R1 — 고교의 손.**
입자 i의 매 걸음:
$$\mathbf{v}_i \leftarrow w\,\mathbf{v}_i + c_1 r_1 (\mathbf{p}_i - \mathbf{x}_i) + c_2 r_2 (\mathbf{g} - \mathbf{x}_i), \qquad \mathbf{x}_i \leftarrow \mathbf{x}_i + \mathbf{v}_i$$
한 글자씩:
- **xᵢ**: 입자 i의 지금 위치(화살표/좌표).
- **vᵢ**: 입자 i의 속도(다음에 얼마나·어디로 움직일지).
- **←**: "왼쪽 값을 오른쪽으로 새로 덮어쓴다"(대입). 등호가 아니라 *갱신* 표시.
- **w**: 관성 무게(inertia). "가던 방향을 얼마나 유지할까". 크면 멀리 헤맴(탐험), 작으면 빨리 가라앉음(집중).
- **pᵢ**: 입자 i의 *개인 최고* 위치(personal best, pbest). 지금껏 자기가 본 가장 좋은 곳.
- **g**: *떼 전체* 최고 위치(global best, gbest). 모든 입자 통틀어 가장 좋은 곳.
- **(pᵢ − xᵢ)**, **(g − xᵢ)**: "지금 위치에서 그 좋은 곳을 향하는 화살표"(끌림 방향).
- **c₁, c₂**: 끌림 세기(인지·사회 계수). c₁은 자기 기억 쪽, c₂는 떼 명당 쪽 끌림.
- **r₁, r₂**: 매번 0~1 사이로 *무작위* 뽑는 수. 끌림에 흔들림을 줘 한 줄로 몰리지 않게 한다.

통째 의미: "관성으로 가던 만큼 가되, 내 최고와 떼 최고 쪽으로 *무작위 강도로* 살짝 휜다." 이 세 힘의 균형이 PSO의 전부다.

▲ **R2로 — 이유:** 이 갱신이 *수렴하는지*, 어떤 조건에서 폭발(발산)하는지 따져야 한다.

**R2 — 학부의 머리.**
입자 하나의 갱신식은 선형 차분방정식(동역학계, 지구08)이고, 그 안정성은 계수 w, c₁, c₂에 달렸다. 안정(수렴) 조건은 대략 **w < 1**, 그리고 **c₁ + c₂ < 4(1+w)** 부근 — 이를 어기면 속도가 매 걸음 커져 입자가 공간 밖으로 *날아가 버린다*(발산). 여기서 함정이 또 박힌다: PSO는 보통 *전역최소를 찾는다는 증명이 없다.* "확률적으로 잘 모인다"는 경험 관찰이지, 보장이 아니다. 운 나쁘면 떼 전체가 가짜 골짜기 g에 *조기수렴(premature convergence)*해 굳어버린다 — **빨리 모였지만 틀린 곳에.** (함정: 빠름 ≠ 정답.)

▲ **R3로 — 이유:** 다른 군집·진화 기법과의 관계, 어느 문제에 맞는지 봐야 한다.

**R3 — 대학원의 시야.**
PSO는 "집단 기반 메타휴리스틱" 가족 — 유전 알고리즘(genetic algorithm), 개미군집(ant colony), 차분진화(differential evolution)의 사촌이다. 어닐링과의 차이: 어닐링은 *한 점·시간으로 식힘*, PSO는 *여러 점·공간으로 정보 공유*. 미분이 필요 없어 "함수가 들쭉날쭉하고 미분이 정의 안 되는" #16의 거친 지형에서 쓴다. 단, 변수가 수만 개로 늘면 "차원의 저주"로 입자가 텅 빈 공간을 헤매 효율이 급락한다.

▲ **R4로 — 이유:** "어떤 문제에서 PSO가 경사법보다 *확실히* 나은가"의 일반 답이 없는 곳으로.

**R4 — 최전선의 칼날.**
"공짜 점심은 없다(No Free Lunch) 정리"가 말하길, 모든 문제에 두루 최고인 최적화는 없다 — PSO가 어떤 문제군에서 빛나고 어떤 데서 지는지를 *문제 보기 전에* 아는 일반법은 없다. 매개변수 w, c₁, c₂를 문제마다 어떻게 정해야 최적인지의 이론적 처방도 — **여기서부터 인류가 모른다.** 대부분 손으로 튜닝한 경험칙이다.

**【정밀층 — 집필표준 v2】**
[락] 본 카드에서 xᵢ·vᵢ는 입자 위치·속도, pᵢ는 개인 최고, g는 떼 최고, w는 관성 무게(고유값·라그랑주 승수 아님), c₁·c₂는 끌림 세기.
R1° 정의역·공역: PSO는 사상 (각 입자 상태 {xᵢ,vᵢ}, 기억 {pᵢ,g}) ↦ 갱신된 {xᵢ,vᵢ} — 흩어진 입자 떼를 매 걸음 명당 쪽으로 끌어 모은다. 속도 갱신 vᵢ ← w vᵢ + c₁r₁(pᵢ−xᵢ) + c₂r₂(g−xᵢ). 케네디·에버하트(1995)가 새 떼 동역학에서 발명.
R2° 보조정리: Lemma(안정성/수렴 조건). 한 입자의 갱신은 선형 차분방정식이고, 그 특성근의 절댓값이 1 미만이어야 입자가 발산 않고 모인다. 귀류법: 만약 w≥1 또는 c₁+c₂가 4(1+w)를 크게 넘으면 특성근 |z|>1이 되어 vᵢ가 매 걸음 |z|배로 커지므로 입자가 공간 밖으로 폭주한다 — 모임과 모순. 따라서 w<1, c₁+c₂<4(1+w) 부근이 안정.
R3° 구조 편입: PSO는 집단 기반 메타휴리스틱(유전·개미·차분진화) 가족이고, 입자 갱신은 감쇠 조화진동자(동역학계)의 이산판. 대수↔해석 사상: 속도 갱신의 선형 차분 대수가 "동역학계 안정성의 해석"과 잇닿아, 특성근으로 탐험(발산 직전)↔수렴(빠른 가라앉음)을 조절.
R4° 파산(수식): 전역 보장 부재 + 조기수렴 — PSO에는 전역최소 수렴의 증명이 없고("확률적으로 잘 모인다"는 경험), 떼 전체가 가짜 골짜기 g에 조기수렴하면 (pᵢ−xᵢ)→0, (g−xᵢ)→0 이라 속도가 vᵢ → w·vᵢ → 0 으로 죽어 그 점에 굳는다. 게다가 No Free Lunch 정리상 모든 문제에 두루 최적인 (w,c₁,c₂)는 존재 불능 → 빠른 모임이 틀린 곳일 수 있다.
쉽게: 새 떼가 "내 명당"과 "떼 명당" 두 쪽으로 끌려 함께 좋은 자리로 모이지만, 운 나쁘면 다 같이 가짜 명당에 빨리 굳어버린다.

### ⑤ 새 의문
어닐링·PSO는 "미분을 안 쓰고 무작정 탐색"한다 — 함수가 *매끈할 때*조차 그 매끈함(기울기 정보)을 버리니 낭비다. **함수가 매끈하고, 특히 "관측값과 모델값의 차이를 줄이는" 모양일 땐, 그 구조를 *정면으로 이용해* 훨씬 빨리 답에 가도록 못 하나?** → 다음 도구 **비선형최소제곱**의 ①로. 닿는 난제: #16, #19.

**[도구 창조 씨앗]** 규칙 "g(떼 최고)는 *하나*"를 "이웃별 *지역 최고*가 여럿"으로 뒤집으면? → 지역 토폴로지 PSO, 조기수렴을 늦춤. 위험 검문소: **④표적** — 지역 최고를 여럿 두면 탐험은 늘지만, *어느 지형에서 정말 조기수렴 벽을 깨는지*가 모호해진다(표적이 흐려짐).

---

> 〔뿌리 줄기〕 **방금 PSO로 "여러 구슬이 정보를 나누며 함께 모이는 탐색"을 얻어, 탐색을 흩는 줄기를 마쳤다.** 그런데 어닐링·PSO는 함수가 *매끈할 때조차* 기울기를 안 쓰고 무작정 탐색한다 — 매끈함을 버리는 낭비다. 여기서 줄기가 "한 걸음을 똑똑히"로 갈라진다.
> ```
> PSO(무작정 탐색) ─── 탐색 흩기 줄기 마감
>  ├─ 줄기①(다음 도구): 매끈함(기울기)을 정면 이용, 관측-모델 차이 줄이기 → 비선형최소제곱
>  ├─ 곁가지: 군집의 조기수렴(한곳에 뭉침) → 함정 "빠름 ≠ 정답"
>  └─ 곁가지: 다파라미터 추정 → 난제 #19
> ```
> "무작정 흩는 대신, 함수가 매끈하면 그 *기울기 구조*를 정면으로 쓰자" — 다음 카드가 그 길을 벼린다.

---

─────────────  ▶ 문제 트레이닝: 입자군집최적화(PSO)  ─────────────

【예제】
 예제 1. 1차원에서 입자 위치 x=2, 속도 v=1, 개인최고 p=5, 떼최고 g=6. w=0.5, c₁=c₂=1, 이번 무작위 r₁=r₂=1(쉽게). 새 속도 v는?
   풀이: v ← w·v + c₁r₁(p−x) + c₂r₂(g−x) = 0.5·1 + 1·1·(5−2) + 1·1·(6−2) = 0.5 + 3 + 4 = 7.5. 이유: 관성 + 내 최고로 끌림 + 떼 최고로 끌림을 그대로 더한다. ∴ v=7.5.
 예제 2. 그 입자의 새 위치 x는? (위 결과 이어서)
   풀이: x ← x + v = 2 + 7.5 = 9.5. 이유: 속도를 위치에 더해 한 걸음 옮긴다. ∴ x=9.5.
 예제 3. 같은 입자가 다음 걸음엔 x=9.5에 도착해 p, g가 그대로 5, 6이면 끌림 (p−x), (g−x)는?
   풀이: (p−x)=5−9.5=−4.5, (g−x)=6−9.5=−2.5 — 둘 다 음수(되돌아오는 방향). 이유: 명당을 지나쳐 갔으니 화살표가 반대로 당긴다. ∴ −4.5, −2.5.

【문제】
 ◇기초 1. (p−x)에서 p=8, x=3이면 끌림 화살표 값은? 2. w=0.4, v=2이면 관성 항 w·v는?
 ◇기본 3. x=1, v=2, p=4, g=5, w=0.5, c₁=c₂=1, r₁=r₂=1. 새 속도 v를 구하라.
        4. 3번에서 얻은 v로 새 위치 x를 구하라.
 ◇유형 5. 안정(수렴) 조건은 w<1 그리고 c₁+c₂<4(1+w) 부근이다. w=0.7일 때 c₁+c₂의 상한은?
        6. w=0.9, c₁=2.5, c₂=2.5. 안정 조건 c₁+c₂<4(1+w)를 만족하나? 위반이면 입자에 무슨 일이?
 ◇심화 7. 1차원 입자 x=0, v=0(정지), p=3, g=3(둘 다 같은 명당), w=0.5, c₁=c₂=1, r₁=r₂=1. 새 v와 새 x를 구하라. 이 입자는 명당으로 다가가나?
        8. 떼 전체가 한 점 g에 모여 모든 입자에서 x=p=g가 됐다(조기수렴). 끌림 항 (p−x), (g−x)는 각각 얼마? 다음 속도는 v←w·v로 어떻게 변하나(w=0.5, 현재 v=0.8)? 세 걸음 뒤 v는?
 ◇최상위 9. 8번처럼 떼가 가짜 골짜기 g에 굳었다. (p−x)=(g−x)=0이라 속도가 v←w·v로 매 걸음 w배씩 줄어 0으로 죽는다(w<1). 이때 입자는 g를 *영영 못 벗어난다.* 만약 더 깊은 진짜 골짜기가 다른 곳에 있다면 PSO는 그걸 찾을까? "빠른 모임"과 "옳은 답"이 왜 다른지 이 식으로 설명하라.

【해설】
 1. (p−x)=8−3=5. 끌림은 "그 좋은 곳을 향하는 화살표"=목표−현위치. 흔한 실수: x−p로 빼서 부호 반대. 답: 5.
 2. w·v=0.4·2=0.8. 관성=무게×속도. 답: 0.8.
 3. v=0.5·2 + 1·(4−1) + 1·(5−1) = 1 + 3 + 4 = 8. 갱신식에 그대로 대입. 흔한 실수: (p−x) 대신 p만 더하기. 답: v=8.
 4. x=1+8=9. 위치=옛 위치+새 속도. 답: x=9.
 5. 4(1+w)=4(1+0.7)=4·1.7=6.8. c₁+c₂는 6.8보다 작아야 안정. 답: 상한 6.8.
 6. c₁+c₂=5, 4(1+0.9)=4·1.9=7.6. 5<7.6이라 *만족*. 위반이었다면 특성근 절댓값이 1을 넘어 속도가 매 걸음 커져 입자가 공간 밖으로 발산(폭주)한다. 답: 만족(5<7.6), 위반 시엔 발산.
 7. v=0.5·0 + 1·(3−0) + 1·(3−0) = 0+3+3 = 6. x=0+6=6. 이 입자는 명당 3을 향해 출발하지만 한 걸음에 6까지 *지나쳐* 간다(과속). 다음 걸음엔 (3−6)=−3로 되당겨져 진동하며 3 둘레로 모인다. 답: v=6, x=6, 진동하며 다가감.
 8. (p−x)=0, (g−x)=0(다 같은 점). 끌림이 사라져 v←w·v=0.5·0.8=0.4, 다음 0.5·0.4=0.2, 다음 0.5·0.2=0.1. 세 걸음 뒤 v=0.1. 속도가 절반씩 줄어 0으로 죽는다. 답: 끌림 0, 세 걸음 뒤 v=0.1(점점 0으로).
 9. (p−x)=(g−x)=0이라 두 끌림이 모두 0 → 속도는 오직 v←w·v로만 줄어 0에 수렴, 입자는 g에 *완전히 굳는다*. 다른 곳의 더 깊은 골짜기를 끌어줄 힘이 식에 남지 않으므로 PSO는 그걸 *못 찾는다*. "빠른 모임"은 끌림이 0이 되어 멈춘 상태일 뿐, 그 g가 *진짜 최저*라는 보장은 식 어디에도 없다 — 이것이 조기수렴, 빠름≠정답. 답: 못 찾는다, 끌림이 0이라 빠르게 굳지만 그 점이 옳다는 보장은 없음.

【정답】 1) 5  2) 0.8  3) v=8  4) x=9  5) 6.8  6) 만족(5<7.6)  7) v=6, x=6, 진동하며 다가감  8) 끌림 0, 세 걸음 뒤 v=0.1  9) 못 찾음 — 조기수렴, 빠름≠정답
【발명의 씨앗】 기초~유형까지는 갱신식 v←w·v+c₁r₁(p−x)+c₂r₂(g−x)에 숫자만 넣으면 새 속도·위치가 또렷이 나왔고, 안정 조건 c₁+c₂<4(1+w)도 부등식 하나로 깔끔했다. 심화 8에서 "떼가 한 점에 모이면 끌림이 0이 된다"는 미묘함이 드러나고, 최상위 9에서 벽이 터졌다: 끌림이 0이면 속도가 v←w·v로 죽어 입자가 g에 굳는데, 그 g가 *가짜 골짜기*여도 식에는 빠져나갈 힘이 없다. 이 공식은 "떼가 진짜 명당으로 모일 땐 완벽한데, 가짜 명당에 먼저 뭉치면" 전역 보장이 □무너진다(No Free Lunch). 그 무너짐이 폭발하는 곳 = 난제 #16(거대 최적화)·#19(다파라미터 추정). 이 난제용 도구란, "빠르게 모이되 틀린 곳에 굳지 않는" 이 불편함을 메우는 것.

---

## ▣ 비선형최소제곱 / Nonlinear Least Squares   지구15 · 선행: 지구07 최적화, 지구04 회귀 → 잠금해제: #19

### ① 의문
(PSO ⑤에서 받음.) 현실 문제의 큰 덩어리는 "모델이 관측을 맞추게 매개변수를 고르는" 일이다 — 예: 측정한 점 100개를 곡선 하나로 맞추기(fitting). **모델 예측과 실제 관측의 *차이(잔차)*들을 한꺼번에 가장 작게 만드는 매개변수를, 그 '차이의 제곱합'이라는 구조를 이용해 똑똑히 찾을 수 없을까?**

### ② 벽
일반 최적화 도구(경사하강)는 "비용이 뭐든 상관없이" 아래로 내려간다 — 비용이 *제곱들의 합*이라는 특별한 구조를 안 쓴다. 그래서 느리다. 또 모델이 매개변수에 *비선형*으로 얽히면(예: 지수·삼각함수 안에 매개변수가 들어감), 차이를 0으로 만드는 식을 *대수로 못 푼다* — 선형회귀의 깔끔한 공식이 무너진다.

```
   잔차(차이) rᵢ = 관측yᵢ − 모델f(xᵢ, θ)
      y │   ·         관측점 ·들
        │  · ╲  rᵢ↕ ·   ·        ← 각 점에서 모델곡선까지 세로 거리 rᵢ
        │ ·   ╲___·___·          이 rᵢ들의 제곱을 다 더한 게 비용
        │___________________ x   θ(매개변수)를 비선형으로 바꿔 곡선을 움직임
```

### ③ 발명
비용을 **잔차 제곱합** S(θ) = Σ rᵢ(θ)² 로 정의한다. 여기서 잔차 **rᵢ(θ) = yᵢ − f(xᵢ, θ)** = "i번째 관측과 모델 예측의 차이". 핵심 발명: 이 *제곱합 구조*를 이용하면, 기울기와 곡률을 잔차의 1차 미분(자코비안 **J**)만으로 *근사*할 수 있다 — 2차 미분(헤시안)을 직접 안 구해도 된다. 새 기호: 자코비안 **J**, 정규방정식 **JᵀJ Δθ = −Jᵀr**.

### ④ 해부

**R0 — 중2의 눈.**
점 여러 개를 가장 잘 지나는 곡선을 그리고 싶다. "잘 지난다"를 "각 점에서 곡선까지 세로 거리(차이)를 재서, 그 거리들을 제곱해 다 더한 값이 가장 작다"로 정한다(제곱은 음수 부호를 없애고, 큰 차이를 더 미워한다). 곡선 모양을 정하는 손잡이(매개변수)를 조금씩 돌려 이 합을 줄여간다.

▲ **R1로 — 이유:** "차이의 제곱합"과 "손잡이를 어느 쪽으로 돌릴지"를 식으로 적어야 한다.

**R1 — 고교의 손.**
$$S(\theta) = \sum_{i=1}^{m} r_i(\theta)^2, \qquad r_i(\theta) = y_i - f(x_i, \theta)$$
한 글자씩:
- **θ**: "세타". 모델의 매개변수(손잡이) 묶음. 곡선 모양을 정한다.
- **Σ (시그마)**: "i=1부터 m까지 다 더하라". 관측이 m개면 잔차 m개를 더한다.
- **rᵢ(θ)**: i번째 잔차 = 관측 yᵢ 빼기 모델 예측 f(xᵢ, θ). θ에 따라 달라지므로 (θ)를 붙인다.
- **f(xᵢ, θ)**: 입력 xᵢ를 매개변수 θ로 변환한 모델의 예측값.
- **S(θ)**: 잔차들의 제곱합. 이게 *작을수록* 모델이 관측을 잘 맞춘 것.

오르막 화살표(그래디언트)는 ∇S = 2 Jᵀ r 꼴인데, 여기서 **J**가 핵심이다(다음 단에서 해부).

▲ **R2로 — 이유:** 자코비안 J가 정확히 뭐고, 왜 *제곱 구조* 덕에 2차 미분 없이 진행하는지 봐야 한다.

**R2 — 학부의 머리.**
**자코비안(Jacobian) J**: m개 잔차 각각을 n개 매개변수 각각으로 편미분한 표(m×n 행렬). **J의 (i,j)칸 = ∂rᵢ/∂θⱼ** = "j번째 손잡이를 살짝 돌리면 i번째 차이가 얼마나 변하나". (∂는 "편미분 round-d", *여러 변수 중 하나만* 살짝 움직였을 때의 변화율 — 지구의 다른 변수는 고정.) 못박기: 이 지구에서 **J는 자코비안**이지 비용함수가 아니다(비용은 S로 쓴다).

비용을 θ 근처에서 잔차의 1차 근사 r(θ+Δθ) ≈ r + JΔθ 로 펴 넣고 최소화하면:
$$J^\top J\,\Delta\theta = -J^\top r \quad(\text{가우스-뉴턴 정규방정식})$$
- **Jᵀ**: J의 행과 열을 뒤집은 전치(transpose). ᵀ는 "표를 대각선으로 뒤집어라".
- **JᵀJ**: n×n 행렬, "헤시안(2차 곡률)의 *근사*". 진짜 헤시안엔 잔차의 2차 미분 항이 더 있는데, 잔차가 작으면(모델이 거의 맞으면) 그 항이 작아 *버려도 된다* — 이게 제곱 구조가 주는 선물이다.
- **Δθ**: 매개변수를 *얼마나 옮길지*(다음 한 걸음).
- 통째 의미: "잔차를 1차로 펴서, 가장 잘 줄이는 한 걸음 Δθ를 *선형 방정식* 풀듯 구한다." 비선형 문제를 *한 걸음마다 선형으로* 근사해 반복한다.

여기서 벽이 다시 나타난다: 잔차가 *크거나*(모델이 많이 틀림) JᵀJ가 거의 납작(특이, near-singular)하면, 이 한 걸음이 *엉뚱하게 폭주*해 발산한다. (함정: 가우스-뉴턴은 빠르지만, 그 빠름이 *틀린 방향으로의* 폭주일 수 있다.)

▲ **R3로 — 이유:** 이 폭주를 길들이는 다음 도구(레벤버그-마카트)와의 연결을 봐야 한다.

**R3 — 대학원의 시야.**
가우스-뉴턴(Gauss-Newton)은 "뉴턴법에서 잔차 2차항을 버린 근사"다. 폭주 문제를 감쇠로 길들인 것이 다음 카드 **레벤버그-마카트**, 더 일반적으로 안전을 보장하는 틀이 그 다음 **신뢰영역**이다. 비선형최소제곱은 #19(다파라미터 추정)의 심장 — GPS 위치 결정, 카메라 보정, 천체 궤도 맞춤이 다 이 꼴이다. J는 자동미분(이 지구 7번 카드)으로 손쉽게 얻는다.

▲ **R4로 — 이유:** "잔차가 클 때·골짜기가 여럿일 때"의 보장 부재로.

**R4 — 최전선의 칼날.**
S(θ)가 *볼록(convex)*이 아니면(골짜기 여럿) 어떤 출발점에서 어느 골짜기로 갈지의 일반 보장이 없다 — 좋은 출발점을 *고르는* 일반법은 문제마다 다르고, 그걸 자동으로 잘 고르는 이론은 — **여기서부터 인류가 모른다.** 또 잔차 2차항을 언제 *버려도 안전한가*의 정량 경계도 부분적으로만 안다.

**【정밀층 — 집필표준 v2】**
[락] 본 카드에서 J는 자코비안(∂rᵢ/∂θⱼ, 비용함수 아님 — 비용은 S), θ는 매개변수, rᵢ는 잔차, S는 잔차 제곱합, ∂는 편미분.
R1° 정의역·공역: 비선형최소제곱은 사상 θ ↦ S(θ)=∑ᵢ rᵢ(θ)²의 최소점 찾기, 한 걸음은 (J,r) ↦ Δθ via 정규방정식 JᵀJ Δθ = −Jᵀr. 매개변수를 잔차 제곱합으로 보내고 그 기울기로 한 걸음을 정한다. 가우스(궤도 맞춤)에서 발명.
R2° 보조정리: Lemma(가우스-뉴턴 정규방정식). 잔차를 1차로 펴 r(θ+Δθ)≈r+JΔθ를 넣고 ‖r+JΔθ‖²를 Δθ로 최소화하면 ∂/∂Δθ[‖r+JΔθ‖²]=2Jᵀ(r+JΔθ)=0 ⟹ JᵀJΔθ=−Jᵀr. 헤시안 ∇²S=2JᵀJ+2∑rᵢ∇²rᵢ에서 잔차 rᵢ가 작으면 둘째 항이 작아 버려도 됨(제곱 구조의 선물).
R3° 구조 편입: 가우스-뉴턴은 뉴턴법(2차)에서 잔차 곡률을 버린 근사이고, JᵀJ는 헤시안의 양의 준정부호 근사. 대수↔해석 사상: 정규방정식 JᵀJΔθ=−Jᵀr가 "선형대수(최소제곱 사영)"을 "비선형 반복 최적화의 해석"으로 잇고, 한 걸음마다 선형으로 근사해 #19 추정에 연결.
R4° 파산(수식): JᵀJ가 거의 특이(near-singular)이면 한 걸음이 폭주 발산 — JᵀJ의 최소 고유값 λ_min → 0이면 Δθ = −(JᵀJ)⁻¹Jᵀr 에서 (JᵀJ)⁻¹의 노름 ~ 1/λ_min → ∞ 라 걸음이 무한대로 튄다. 또 잔차가 클 때 버린 2차항 ∑rᵢ∇²rᵢ가 더는 무시 불능이라 근사가 깨지고, S(θ)가 비볼록이면 어느 국소최소로 갈지 보장 부재.
쉽게: 점들에 곡선을 맞추려 차이의 제곱합을 줄이는데, 기울기 표 JᵀJ가 납작해지면 한 걸음이 엉뚱하게 폭주한다.

### ⑤ 새 의문
가우스-뉴턴 한 걸음은 잔차가 크면 폭주한다. **"멀리 갈 땐 조심조심(경사하강처럼), 가까이 가면 과감히(가우스-뉴턴처럼)" 한 걸음의 크기를 *스스로 조절*하는 손잡이를 달 수 없을까?** → 다음 도구 **레벤버그-마카트**의 ①로. 닿는 난제: #19.

**[도구 창조 씨앗]** 규칙 "잔차를 *제곱*해 더한다"를 "잔차의 *절댓값*을 더한다"로 뒤집으면? → L1 최소화, 이상치(outlier)에 강해짐. 위험 검문소: **②정의** — 절댓값은 0에서 미분이 꺾여(매끈하지 않음) J 기반 한 걸음 식이 *그 자리에서 정의가 흔들린다*(서브그래디언트 필요).

---

> 〔뿌리 줄기〕 **방금 비선형최소제곱으로 "관측-모델 차이를 기울기로 정면 공략하는 가우스-뉴턴 한 걸음"을 얻었다.** 그런데 그 한 걸음은 잔차가 크면 *폭주*한다 — 멀리서 너무 크게 내디뎌 발산한다. 여기서 갈래가 갈라진다.
> ```
> 비선형최소제곱(가우스-뉴턴 걸음)
>  ├─ 줄기①(다음 도구): 멀면 조심·가까우면 과감, 걸음 크기 스스로 조절 → 레벤버그-마카트
>  ├─ 곁가지: 자코비안 J의 의미(출력↔입력 반응 표) → 자동미분(세 카드 뒤)
>  └─ 곁가지: 다물체·다파라미터 추정 → 난제 #19
> ```
> "폭주를 막으려면 *걸음 크기에 손잡이*를 달아야 한다 — 멀 땐 줄이고 가까울 땐 키우는" — 다음 카드가 그 감쇠 손잡이(λ)를 벼린다.

---

─────────────  ▶ 문제 트레이닝: 비선형최소제곱(Nonlinear Least Squares)  ─────────────

【예제】
 예제 1. 관측 y=(3, 5, 8), 모델 예측 f=(2, 6, 7). 잔차 rᵢ=yᵢ−fᵢ와 제곱합 S를 구하라.
   풀이: r=(3−2, 5−6, 8−7)=(1, −1, 1). S=Σrᵢ²=1²+(−1)²+1²=3. 이유: 잔차는 관측−모델, S는 그 제곱합(부호 없애고 큰 차이 더 미워함). ∴ r=(1,−1,1), S=3.
 예제 2. 모델 f(x,θ)=θ·x, 데이터점 (x,y)=(2,7). θ=3일 때 잔차 r과 그 θ에 대한 자코비안 J=∂r/∂θ는?
   풀이: f=3·2=6, r=y−f=7−6=1. ∂r/∂θ = ∂(y−θx)/∂θ = −x = −2. 이유: 잔차를 θ로 편미분하면 −x. ∴ r=1, J=−2.
 예제 3. 예제 2에서 가우스-뉴턴 한 걸음 Δθ를 정규방정식 JᵀJ Δθ=−Jᵀr로 구하라(스칼라).
   풀이: J=−2, r=1. JᵀJ=(−2)²=4. Jᵀr=(−2)(1)=−2. 식: 4·Δθ=−(−2)=2 → Δθ=0.5. 이유: 1차 근사로 잔차를 가장 줄이는 걸음. 새 θ=3+0.5=3.5. 검산: f=3.5·2=7, r=0 (정확히 맞음). ∴ Δθ=0.5.

【문제】
 ◇기초 1. 관측 y=(4,2), 모델 f=(1,3). 잔차 r과 제곱합 S는? 2. 모델 f=θ·x, (x,y)=(3,10), θ=2. 잔차 r은?
 ◇기본 3. 모델 f=θ·x, (x,y)=(3,10), θ=2. 자코비안 J=∂r/∂θ는? 4. 두 점 데이터에서 J=(−1, −2)ᵀ(열벡터)일 때 JᵀJ(스칼라)는?
 ◇유형 5. 1파라미터 직선맞춤 f=θx. 점 (1,2),(2,3). θ=1일 때 잔차벡터 r과 자코비안 J(열벡터)를 구하라. (J의 i칸=∂rᵢ/∂θ=−xᵢ)
        6. 5번 값으로 JᵀJ와 Jᵀr를 구하라.
 ◇심화 7. 5,6번을 이어 정규방정식 JᵀJ Δθ=−Jᵀr로 가우스-뉴턴 걸음 Δθ와 새 θ를 구하라. (선형 모델이라 한 걸음에 최소제곱해에 도달)
        8. 7번의 새 θ에서 잔차 제곱합 S가 처음(θ=1)보다 줄었는지 확인하라. (처음 S와 새 S를 비교)
 ◇최상위 9. 자코비안이 J=(1, 1.0001)ᵀ에 가깝게 두 칸이 거의 같은(거의 평행) 1파라미터가 아니라, 2파라미터 문제에서 J의 두 열이 거의 같아 JᵀJ가 거의 납작(특이)해졌다고 하자. JᵀJ=[[2, 2],[2, 2.0002]](예시)일 때 행렬식 det(JᵀJ)를 구하고, 이게 0에 가까우면 한 걸음 Δθ=−(JᵀJ)⁻¹Jᵀr에 무슨 일이 생기는지(폭주) 설명하라. 가우스-뉴턴의 "빠름"이 왜 위험으로 바뀌나?

【해설】
 1. r=(4−1, 2−3)=(3, −1). S=3²+(−1)²=9+1=10. 잔차=관측−모델. 답: r=(3,−1), S=10.
 2. f=2·3=6, r=10−6=4. 답: r=4.
 3. J=∂(y−θx)/∂θ=−x=−3. 흔한 실수: 부호 빼먹고 +3. 답: J=−3.
 4. JᵀJ=(−1)²+(−2)²=1+4=5. 열벡터의 자기 내적. 답: 5.
 5. f₁=1·1=1, r₁=2−1=1. f₂=1·2=2, r₂=3−2=1. r=(1,1). J=(−x₁,−x₂)=(−1,−2)ᵀ. 답: r=(1,1), J=(−1,−2)ᵀ.
 6. JᵀJ=(−1)²+(−2)²=1+4=5. Jᵀr=(−1)(1)+(−2)(1)=−1−2=−3. 답: JᵀJ=5, Jᵀr=−3.
 7. 5·Δθ=−(−3)=3 → Δθ=3/5=0.6. 새 θ=1+0.6=1.6. 답: Δθ=0.6, θ=1.6.
 8. 처음(θ=1): S=1²+1²=2. 새(θ=1.6): f₁=1.6·1=1.6, r₁=2−1.6=0.4; f₂=1.6·2=3.2, r₂=3−3.2=−0.2. S=0.4²+(−0.2)²=0.16+0.04=0.20. 0.20<2이므로 *줄었다*. (최소제곱해 θ=Σxy/Σx²=(1·2+2·3)/(1+4)=8/5=1.6 확인.) 답: S가 2 → 0.2로 줄었다.
 9. det(JᵀJ)=2·2.0002 − 2·2 = 4.0004 − 4 = 0.0004(거의 0). (JᵀJ)⁻¹는 1/det에 비례하므로 det이 0에 가까우면 역행렬 원소가 1/0.0004=2500처럼 *거대하게 부푼다* → Δθ=−(JᵀJ)⁻¹Jᵀr가 터무니없이 큰 폭주 걸음을 낸다. 두 열이 거의 평행(두 파라미터가 거의 구별 안 됨)이라 "어느 쪽으로 가야 할지"가 거의 정해지지 않아 작은 잡음에도 걸음이 폭발한다. 가우스-뉴턴은 잔차가 작고 J가 멀쩡하면 빠르지만, JᵀJ가 납작해지는 순간 그 "빠른 한 걸음"이 발산으로 돌변한다. 답: det≈0.0004, 역행렬 폭발 → Δθ 폭주(빠름이 발산으로).

【정답】 1) r=(3,−1), S=10  2) r=4  3) J=−3  4) 5  5) r=(1,1), J=(−1,−2)ᵀ  6) JᵀJ=5, Jᵀr=−3  7) Δθ=0.6, θ=1.6  8) S: 2→0.2(줄었다)  9) det≈0.0004 → 역행렬 폭발, Δθ 폭주
【발명의 씨앗】 기초~심화까지는 잔차 r=y−f, 제곱합 S=Σr², 정규방정식 JᵀJΔθ=−Jᵀr이 손에 척척 붙었다 — 한 걸음에 직선맞춤 최소제곱해(θ=1.6)에 닿고 S가 2에서 0.2로 줄어드는 게 통쾌했다. 그런데 최상위 9에서 벽이 터졌다: J의 두 열이 거의 평행해 JᵀJ가 거의 납작(det≈0)해지면, 한 걸음 Δθ=−(JᵀJ)⁻¹Jᵀr이 역행렬 폭발로 *엉뚱하게 발산*한다. 이 공식은 "JᵀJ가 멀쩡하고 잔차가 작을 땐 완벽한데, 두 파라미터가 거의 구별 안 되면(조건수 나쁨)" 한 걸음이 □폭주한다. 그 무너짐이 폭발하는 곳 = 난제 #19(다물체·다파라미터 추정). 이 난제용 도구란, "납작한 JᵀJ에서도 폭주 않고 안전히 한 걸음 내딛는" 이 불편함을 메우는 것 — 다음 카드의 감쇠 λ가 그 첫 시도다.

---

## ▣ 레벤버그-마카트 / Levenberg-Marquardt   지구15 · 선행: ← 비선형최소제곱 → 잠금해제: #19

### ① 의문
(비선형최소제곱 ⑤에서 받음.) 가우스-뉴턴은 가까울 때 빠르지만 멀 때 폭주하고, 경사하강은 안전하지만 느리다. **이 둘을 *하나의 손잡이*로 매끄럽게 오가게 — 멀면 안전한 경사하강, 가까우면 빠른 가우스-뉴턴 — 자동 전환할 수 없을까?**

### ② 벽
가우스-뉴턴의 정규방정식 JᵀJ Δθ = −Jᵀr는, JᵀJ가 거의 납작(특이)하면 Δθ가 *터무니없이 큰 폭주 걸음*을 낸다 — 역행렬이 거의 무한대로 부푼다. 경사하강은 안전하지만 골짜기 바닥 근처에서 *지그재그로 기어가* 느리다. 둘 사이를 *연속적으로* 조절하는 손잡이가 없다.

```
   JᵀJ가 납작할 때:           대각선에 λ를 더하면:
   [거의 0] → 역행렬 폭발      [JᵀJ + λI] → 0에서 멀어져 안정
       ↓ 폭주 걸음                ↓ λ 크면 작고 안전한 걸음(경사하강 쪽)
       ✗                          ✓ λ 작으면 큰 걸음(가우스-뉴턴 쪽)
```

### ③ 발명
정규방정식의 JᵀJ에 **감쇠 인자 λ**를 곱한 단위행렬을 *더한다*. 새 방정식: **(JᵀJ + λI) Δθ = −Jᵀr**. λ가 크면 걸음이 작고 경사하강을 닮고, λ가 작으면 가우스-뉴턴을 닮는다. 매 걸음 성공하면 λ를 줄이고(과감), 실패하면 λ를 키운다(조심). 이 *적응 손잡이* λ가 발명의 핵심이다.

### ④ 해부

**R0 — 중2의 눈.**
운전을 생각하자. 길이 안 보이면(멀면) 천천히·짧게 간다. 길이 또렷이 보이면(가까우면) 과감히 길게 간다. λ는 "지금 얼마나 조심할까"의 브레이크 세기다. 한 걸음 가보고 좋아지면 브레이크를 풀고(λ↓), 나빠지면 브레이크를 더 밟는다(λ↑).

▲ **R1로 — 이유:** "브레이크"를 식 어디에 어떻게 넣는지 적어야 한다.

**R1 — 고교의 손.**
$$(J^\top J + \lambda I)\,\Delta\theta = -J^\top r$$
한 글자씩:
- **J, Jᵀ, JᵀJ, r, Δθ**: 앞 카드와 같음(J=자코비안, r=잔차, Δθ=걸음). (→ 비선형최소제곱 참조)
- **λ (람다)**: 못박기 — 여기서 λ는 **감쇠 인자(damping factor)**다. 고유값이 *아니다*. "얼마나 조심해서 짧게 걸을까"의 브레이크 숫자(λ≥0).
- **I**: 단위행렬(항등행렬), 대각선만 1·나머지 0. "JᵀJ의 대각선만 λ만큼 부풀려라"는 뜻.
- **λI를 더하면**: JᵀJ가 거의 납작이어도 대각선이 λ만큼 솟아 *역행렬이 폭발하지 않는다*. λ가 아주 크면 (JᵀJ+λI)≈λI라 Δθ ≈ −(1/λ)Jᵀr — 이건 *작은 경사하강 걸음*. λ가 0에 가까우면 원래 가우스-뉴턴.

통째 의미: "한 손잡이 λ로 경사하강(안전·느림)과 가우스-뉴턴(과감·빠름) 사이를 매끄럽게 돈다."

▲ **R2로 — 이유:** λ를 *언제 키우고 줄이는지*의 규칙과, 이게 신뢰영역의 특수경우임을 봐야 한다.

**R2 — 학부의 머리.**
적응 규칙: 시험 걸음을 가서 S(θ+Δθ) < S(θ)면(좋아짐) 받아들이고 λ ← λ/ν (예 ν=10, 과감하게), 나빠지면 걸음을 버리고 λ ← λ·ν (조심하게) 후 재시도. **핵심 통찰: 각 λ는 "한 걸음 크기를 어떤 반지름 안으로 제한한 것"과 수학적으로 같다** — 즉 레벤버그-마카트는 신뢰영역(다음 카드)의 한 구현이다. 함정 못박기: λ를 키우면 *항상 비용이 줄어드는 걸음*을 결국 찾지만, 그렇다고 *전역최소*에 간다는 보장은 없다 — 가장 가까운 국소최소로 *안정적으로* 갈 뿐이다. ("안정적 빠름"이지 "전역 정답"이 아니다.)

▲ **R3로 — 이유:** 더 일반적인 안전 틀(신뢰영역)과의 관계를 봐야 한다.

**R3 — 대학원의 시야.**
레벤버그-마카트는 "신뢰영역 + 가우스-뉴턴 모델"의 가장 유명한 사례다. λ↔신뢰반지름 Δ는 일대일로 대응한다(λ 크면 반지름 작음). 곡선 맞춤·기계학습의 작은 비선형 회귀·로봇 캘리브레이션에서 #19의 표준 도구다. J는 자동미분(7번 카드)으로, JᵀJ 풀이는 크릴로프(10번 카드)로 가속한다.

▲ **R4로 — 이유:** "λ 갱신 규칙의 최적 일반형"이 없는 곳으로.

**R4 — 최전선의 칼날.**
λ를 정확히 *얼마씩* 키우고 줄여야 가장 빠른가의 보편 최적 규칙은 없다 — ν=10은 경험칙이다. 잔차가 크고 모델이 심하게 비선형일 때 수렴 속도의 일반 보장도 부분적이다. 어디까지가 "안전하게 빠른" 경계인지는 — **여기서부터 정량적으로는 인류가 다 모른다.**

**【정밀층 — 집필표준 v2】**
[락] 본 카드에서 λ는 감쇠 인자(damping factor)이지 고유값·라그랑주 승수·파장이 아니다. J는 자코비안, r은 잔차, I는 단위행렬, Δθ는 걸음.
R1° 정의역·공역: 레벤버그-마카트는 사상 (J, r, λ) ↦ Δθ via (JᵀJ+λI)Δθ = −Jᵀr — 감쇠 손잡이 λ로 경사하강(λ 큼)↔가우스-뉴턴(λ 작음)을 매끄럽게 잇는다. 레벤버그(1944)·마카트(1963)가 발명.
R2° 보조정리: Lemma(λI가 폭주를 막음). JᵀJ가 거의 특이여도 (JᵀJ+λI)의 최소 고유값 ≥ λ > 0 이라 역행렬 노름이 ≤1/λ로 묶인다. 검증: λ→∞면 (JᵀJ+λI)≈λI라 Δθ≈−(1/λ)Jᵀr(작은 경사하강 걸음), λ→0이면 원래 가우스-뉴턴. 적응 규칙(개선되면 λ↓, 나빠지면 λ↑)이 항상 비용 감소 걸음을 결국 찾는다.
R3° 구조 편입: 레벤버그-마카트는 신뢰영역법의 한 구현이고, λ는 신뢰반지름 Δ의 라그랑주 승수(λ 크면 Δ 작음, 일대일 대응). 대수↔해석 사상: (JᵀJ+λI)⁻¹의 능형(ridge) 정규화 대수가 "신뢰영역 제약 ‖Δθ‖≤Δ의 해석"과 같아, 두 도구가 한 몸으로 #19 추정에 연결.
R4° 파산(수식): 국소최소만 보장(전역 아님) — λ를 키우면 항상 S 감소 걸음을 찾지만, 그건 ∇S=0인 가장 가까운 정류점(국소최소)으로의 안정 수렴일 뿐 전역최소 보장이 아니다. S가 비볼록(골짜기 여럿)이면 출발점이 속한 골짜기 하나에 묶이고, λ 갱신 인자 ν의 최적값은 보편 처방 부재 → "안정적 빠름"이 "전역 정답"을 약속하지 않는다.
쉽게: 길이 안 보이면 천천히(λ 크게), 또렷하면 과감히(λ 작게) 가는 브레이크 손잡이인데, 결국 가장 가까운 골짜기로 안전히 갈 뿐 가장 깊은 바닥은 아닐 수 있다.

### ⑤ 새 의문
레벤버그-마카트는 λ를 "걸음 크기"로 *간접* 조절한다. **차라리 "이번엔 이 반지름 안에서만 믿고 움직이자"라고 *직접* 신뢰 범위를 정하고, 그 안에서 최선의 걸음을 푸는 더 일반적인 틀을 세울 수 없을까?** → 다음 도구 **신뢰영역방법**의 ①로. 닿는 난제: #19, #22.

**[도구 창조 씨앗]** 규칙 "λI(대각선 균일 감쇠)"를 "λ·diag(JᵀJ)(대각선 *비례* 감쇠)"로 뒤집으면? → 마카트의 원래 변형, 스케일 다른 변수에 강함. 위험 검문소: **①환원** — 비례 감쇠는 변수 스케일에 의존해, "잘 정규화된 쉬운 경우"에서 균일 감쇠와 *다른 답*으로 갈 수 있어 환원 일관성이 흔들린다.

---

> 〔뿌리 줄기〕 **방금 레벤버그-마카트로 "감쇠 손잡이(λ)로 걸음 크기를 *간접* 조절하는 법"을 얻었다.** 그런데 λ는 걸음 크기를 빙 둘러 조절한다 — 차라리 "이 반지름 안에서만 믿고 움직이자"고 *직접* 범위를 정하면 더 깔끔하지 않을까? 여기서 갈래가 갈라진다.
> ```
> 레벤버그-마카트(λ로 간접 조절)
>  ├─ 줄기①(다음 도구): 신뢰 반지름을 직접 정하고 그 안에서 최선 → 신뢰영역방법
>  ├─ 곁가지: λ의 두 얼굴(감쇠 vs 라그랑주 승수) → 못박기 재확인
>  └─ 곁가지: 비선형 동적평형 제어 → 난제 #22
> ```
> "걸음을 *간접*으로 조이는 대신, *직접* 믿을 범위를 긋자" — 다음 카드가 그 더 일반적인 틀을 벼린다.

---

─────────────  ▶ 문제 트레이닝: 레벤버그-마카트(Levenberg-Marquardt)  ─────────────

【예제】
 예제 1. 스칼라 문제 JᵀJ=4, Jᵀr=−2, 감쇠 λ=0. (JᵀJ+λI)Δθ=−Jᵀr로 걸음 Δθ는?
   풀이: (4+0)Δθ=−(−2)=2 → Δθ=0.5. 이유: λ=0이면 가우스-뉴턴 그대로. ∴ Δθ=0.5.
 예제 2. 같은 문제에 λ=4를 넣으면 Δθ는? 걸음이 커졌나 작아졌나?
   풀이: (4+4)Δθ=2 → 8Δθ=2 → Δθ=0.25. λ를 키우니 걸음이 0.5→0.25로 *작아졌다*(조심해짐, 경사하강 쪽). ∴ Δθ=0.25.
 예제 3. λ가 아주 클 때(λ=96) Δθ는? 작은 경사하강 걸음 −(1/λ)Jᵀr=−(1/96)(−2)와 비교하라.
   풀이: (4+96)Δθ=2 → 100Δθ=2 → Δθ=0.02. 근사 −(1/λ)Jᵀr=−(1/96)(−2)=2/96≈0.0208. 거의 같다 — λ가 크면 (JᵀJ+λI)≈λI라 가우스-뉴턴이 *작은 경사하강 걸음*으로 변한다. ∴ Δθ=0.02.

【문제】
 ◇기초 1. JᵀJ=5, Jᵀr=−10, λ=0. Δθ는? 2. λ=0일 때 레벤버그-마카트는 어떤 방법과 같아지나?
 ◇기본 3. JᵀJ=2, Jᵀr=−6, λ=4. (JᵀJ+λI)Δθ=−Jᵀr로 Δθ는? 4. 3번에서 λ를 0으로 바꾸면 Δθ는? λ가 걸음 크기에 준 효과를 한 줄로.
 ◇유형 5. 적응 규칙: 시험 걸음이 비용을 줄이면 λ←λ/10, 늘리면 λ←λ·10. 시작 λ=1. (좋아짐, 나빠짐, 좋아짐) 순서로 갱신하면 매번 λ는?
        6. JᵀJ=3, Jᵀr=−3. λ=0, λ=1, λ=3에서 Δθ를 각각 구하고, λ가 커질수록 걸음 크기가 어떻게 변하는지 답하라.
 ◇심화 7. JᵀJ가 거의 납작: JᵀJ=0.01, Jᵀr=−1. λ=0이면 Δθ=? (폭주 확인) λ=1을 더하면 Δθ=? λI가 폭주를 어떻게 막았나?
        8. 2×2: JᵀJ=[[2,0],[0,0]](둘째 방향이 완전히 납작), Jᵀr=(−2,−2)ᵀ. λ=0이면 둘째 성분 Δθ₂는 (0)Δθ₂=2라 *풀 수 없다*(특이). λ=1을 더해 (JᵀJ+I)Δθ=−Jᵀr를 풀어 Δθ를 구하라.
 ◇최상위 9. 레벤버그-마카트는 λ를 키우면 *항상* 비용 줄이는 걸음을 결국 찾아 가장 가까운 골짜기로 안전히 수렴한다. 그런데 비용 S(θ)에 골짜기가 둘 있고(θ≈1에 얕은 것, θ≈9에 깊은 것), 출발점이 θ=0이라 하자. 레벤버그-마카트는 어느 골짜기로 갈까? "안정적 빠름"이 왜 "전역 정답"을 보장하지 않는지 이 상황으로 설명하라.

【해설】
 1. (5+0)Δθ=−(−10)=10 → Δθ=2. λ=0=가우스-뉴턴. 답: Δθ=2.
 2. 가우스-뉴턴(λ=0이면 (JᵀJ)Δθ=−Jᵀr 원형). 답: 가우스-뉴턴.
 3. (2+4)Δθ=−(−6)=6 → 6Δθ=6 → Δθ=1. 흔한 실수: −Jᵀr 부호 빠뜨려 −6 쓰기. 답: Δθ=1.
 4. (2+0)Δθ=6 → Δθ=3. λ를 0→4로 키우니 걸음이 3→1로 작아졌다 — λ가 클수록 조심스러운(짧은) 걸음. 답: Δθ=3, λ가 걸음을 줄인다.
 5. 좋아짐: λ=1/10=0.1. 나빠짐: 0.1·10=1. 좋아짐: 1/10=0.1. 답: 0.1, 1, 0.1.
 6. λ=0: 3Δθ=3→Δθ=1. λ=1: 4Δθ=3→Δθ=0.75. λ=3: 6Δθ=3→Δθ=0.5. λ가 커질수록 걸음이 1→0.75→0.5로 작아진다. 답: 1, 0.75, 0.5(λ↑면 걸음↓).
 7. λ=0: 0.01·Δθ=−(−1)=1 → Δθ=1/0.01=100(터무니없이 큰 폭주 걸음). λ=1: (0.01+1)Δθ=1 → 1.01Δθ=1 → Δθ≈0.990. λI가 대각선을 0.01에서 1.01로 솟구쳐 역행렬 폭발(1/0.01=100)을 1/1.01≈0.99로 *눌렀다*. 답: λ=0이면 Δθ=100(폭주), λ=1이면 Δθ≈0.99(길들여짐).
 8. (JᵀJ+I)=[[3,0],[0,1]]. 식: 3Δθ₁=2 → Δθ₁=2/3≈0.667; 1·Δθ₂=2 → Δθ₂=2. λI 덕에 납작했던 둘째 방향도 풀린다(0이 1로 솟음). 답: Δθ=(2/3, 2).
 9. 출발 θ=0에서 가장 가까운 골짜기는 θ≈1(얕은 것)이다. 레벤버그-마카트는 매 걸음 비용을 줄이는 *내리막*만 안전히 따라가므로 θ≈1 골짜기로 빨려 들어가 *거기서 멈춘다* — 더 깊은 θ≈9 골짜기로 건너가려면 *잠깐 오르막*을 넘어야 하는데, 비용 줄이는 걸음만 받아들이는 규칙이 그걸 금지한다. 그래서 "λ를 키우면 항상 줄이는 걸음을 찾는다"는 *안정적 빠름*은 가장 가까운 국소최소를 보장할 뿐, *전역최소(가장 깊은 바닥)*는 보장하지 못한다. 답: 얕은 θ≈1 골짜기로 감 — 국소최소만 보장, 전역 아님.

【정답】 1) Δθ=2  2) 가우스-뉴턴  3) Δθ=1  4) Δθ=3(λ↑면 걸음↓)  5) 0.1, 1, 0.1  6) 1, 0.75, 0.5  7) 100(폭주) → ≈0.99(길들임)  8) Δθ=(2/3, 2)  9) 얕은 θ≈1 골짜기 — 국소최소만 보장
【발명의 씨앗】 기초~심화까지는 (JᵀJ+λI)Δθ=−Jᵀr 한 식에 λ만 바꿔 넣으면 걸음 크기가 매끄럽게 조절됐다 — λ=0이면 과감한 가우스-뉴턴, λ가 크면 작고 안전한 경사하강, 그리고 7·8에서 λI가 납작한 JᵀJ의 폭주(Δθ=100)와 특이성을 깔끔히 길들였다. 비선형최소제곱의 그 폭주가 *메워진* 것이다. 그런데 최상위 9에서 새 벽이 터졌다: λ는 *걸음의 폭주*는 막아도, 비용에 골짜기가 여럿이면 출발점에서 가장 가까운 얕은 골짜기에 안전히 갇힌다. 이 공식은 "한 골짜기 안에서는 완벽히 안전·빠른데, 골짜기가 여럿(비볼록)이면" 전역최소 보장이 □무너진다. 그 무너짐이 폭발하는 곳 = 난제 #19(다파라미터 추정). 이 난제용 도구란, "안전한 한 걸음을 넘어, 가장 깊은 골짜기를 *고르는*" 이 불편함을 메우는 것 — 다음 카드의 신뢰영역이 그 더 일반적인 틀이다.

---

## ▣ 신뢰영역방법 / Trust Region Methods   지구15 · 선행: ← 레벤버그-마카트, 지구07 뉴턴법 → 잠금해제: #19, #22

### ① 의문
(레벤버그-마카트 ⑤에서 받음.) 우리는 비용을 한 점 근처에서 *2차식*으로 근사해 걸음을 정한다. 그런데 그 근사는 *가까이서만* 믿을 만하다. **"이 근사를 어느 반지름까지 믿을지" 신뢰 범위를 *명시적으로* 정하고, 그 안에서만 최선의 걸음을 고른 뒤, 결과를 보고 반지름을 늘리거나 줄일 수 없을까?**

### ② 벽
뉴턴법(Newton's method)은 2차 근사의 *바닥*으로 단번에 점프한다. 근사가 좋으면 빠르지만, 멀면 그 바닥이 엉뚱한 데라 *근사를 너무 믿어* 발산한다(라인서치는 *방향*만 정하고 거리만 줄여, 방향 자체가 나쁘면 못 고친다). "근사를 어디까지 믿을지"라는 *범위* 개념이 빠졌다.

```
   진짜 비용 곡선:    ╲___╱        2차 근사(점선): ⋰‾‾⋱
   믿는 반지름 Δ:   |←─Δ─→|        Δ 안에서만 점선의 바닥을 찾음
        ✗ Δ 너무 크면: 점선 바닥이 진짜와 달라 헛걸음
        ✓ Δ 적당하면: 점선이 진짜와 거의 같은 구역 안에서만 움직임
```

### ③ 발명
각 걸음마다 (가) 비용의 2차 모델 **m(Δθ)**를 만들고, (나) **‖Δθ‖ ≤ Δ**(신뢰반지름) 안에서 m을 최소화하는 걸음을 구한다. (다) 실제 줄어듦 ÷ 모델이 예측한 줄어듦 = **ρ**(신뢰비)를 잰다 — ρ가 1에 가까우면(예측이 잘 맞음) 반지름 Δ를 키우고, ρ가 작으면(빗나감) Δ를 줄인다. 새 기호: 신뢰반지름 **Δ**, 신뢰비 **ρ**.

### ④ 해부

**R0 — 중2의 눈.**
지도가 *내 근처만* 정확하다고 하자. 그럼 "내가 믿는 반경" 안에서만 길을 정해 한 걸음 간다. 가본 뒤, 지도 예측과 실제가 잘 맞았으면 다음엔 더 넓게 믿고(반경↑), 빗나갔으면 더 좁혀 믿는다(반경↓). 이 "믿는 반경을 결과 보고 조절"이 핵심.

▲ **R1로 — 이유:** "2차 모델"과 "반경 조절 규칙"을 식으로 적어야 한다.

**R1 — 고교의 손.**
한 걸음의 부분문제:
$$\min_{\Delta\theta}\; m(\Delta\theta) = f + \mathbf{g}^\top \Delta\theta + \tfrac{1}{2}\Delta\theta^\top H\,\Delta\theta \quad\text{단}\quad \lVert\Delta\theta\rVert \le \Delta$$
한 글자씩:
- **min_{Δθ}**: "Δθ를 골라 아래 값을 최소로". Δθ는 걸음.
- **m(Δθ)**: 비용의 *2차 근사 모델*. 진짜 비용 f를 한 점 근처에서 포물선으로 흉내.
- **f**: 지금 점의 비용값(상수).
- **g**: 그래디언트(∇f), "오르막 화살표". gᵀΔθ는 기울기 방향 1차 항.
- **H**: 헤시안(Hessian), 2차 미분 표 = "곡률(휜 정도)". ½ΔθᵀHΔθ는 포물선의 휨 항.
- **‖Δθ‖**: 걸음의 길이(노름, norm). ‖·‖는 "화살표의 크기를 재는 자".
- **Δ (델타, 대문자)**: 못박기 — 여기 Δ는 *변화량* Δθ의 Δ와 달리 **신뢰반지름(trust radius)**, "이번에 모델을 믿는 최대 거리". 글자가 같아 헷갈리니 "반지름 Δ"라 읽어라.
- **ρ (로)**: 신뢰비 = (실제 줄어든 비용) ÷ (모델이 예측한 줄어든 비용). 못박기 — 여기 ρ는 밀도가 아니라 *예측 정확도 점수*다.

통째 의미: "내가 믿는 반지름 Δ 안에서, 포물선 모델을 가장 낮추는 걸음을 고른다. 가본 뒤 점수 ρ로 반지름을 조절한다."

▲ **R2로 — 이유:** "왜 이게 항상 안전한가(전역 수렴)"를 정리로 봐야 한다.

**R2 — 학부의 머리.**
반지름 조절 규칙: ρ > 3/4면 모델이 잘 맞으니 Δ ← 2Δ(넓게 믿음), ρ < 1/4면 Δ ← Δ/4(좁힘), 그 사이면 유지. ρ ≤ 0(실제론 나빠짐)이면 걸음을 *거절*. 핵심 정리: 온건한 조건(헤시안이 너무 안 부풀고, 부분문제를 충분히 잘 풀면) 아래 신뢰영역법은 **어디서 출발하든 정류점(∇f=0)으로 전역 수렴(global convergence)한다.** 단 못박기: "전역 수렴"은 *전역최소*가 아니라 "어떤 정류점에 도달함을 보장"이라는 뜻이다 — 골짜기가 여럿이면 그중 하나(국소최소)일 수 있다. (함정 정밀화: '전역 수렴'이라는 말이 '전역최소'를 약속하지 *않는다*. 빠르고 안전해도 가장 깊은 바닥은 아닐 수 있다.)

▲ **R3로 — 이유:** 부분문제 푸는 법, 다른 도구와의 연결을 봐야 한다.

**R3 — 대학원의 시야.**
반지름 안 부분문제는 도글레그(dogleg)·스테이하우그(Steihaug, 크릴로프 기반, 10번 카드)·정확한 해(라그랑주 승수로 ‖Δθ‖=Δ 처리) 등으로 푼다 — 레벤버그-마카트의 λI는 바로 이 라그랑주 승수다(λ↔Δ 대응). 신뢰영역은 비선형최적화의 *황금 표준 안전망*이라, #22(비선형 제어·동적평형 유지)의 모델예측제어(MPC)와 #19의 대규모 추정에 쓴다.

▲ **R4로 — 이유:** "비볼록·안장점에서의 최적 반지름 전략" 부재로.

**R4 — 최전선의 칼날.**
헤시안이 음의 곡률을 가질 때(안장점, saddle point) 부분문제를 *정확히* 어떻게 푸는 게 최적인지, 그리고 비볼록 큰 문제에서 안장점을 *빨리 탈출*하는 반지름 전략의 보편 최적형은 — **여기서부터 인류가 다 모른다.** 딥러닝의 거대 비볼록 지형에서 신뢰영역을 *효율적으로* 쓰는 법은 활발한 미해결 연구다.

**【정밀층 — 집필표준 v2】**
[락] 본 카드에서 Δ는 신뢰반지름(변화량 Δθ의 Δ와 구별·고유값 아님), ρ는 신뢰비(예측 정확도 점수, 밀도 아님), g는 그래디언트, H는 헤시안, m은 2차 모델.
R1° 정의역·공역: 신뢰영역은 사상 (g, H, Δ) ↦ Δθ via min_{‖Δθ‖≤Δ} m(Δθ)=f+gᵀΔθ+½ΔθᵀHΔθ, 그리고 결과로 (실제감소/예측감소)=ρ ↦ Δ 갱신. 2차 모델과 믿는 반경을 받아 한 걸음과 새 반경으로 보낸다. 파월·모레 등이 정립.
R2° 보조정리: Lemma(전역 수렴, 정류점으로). 온건 조건(H 유계, 부분문제를 코시점 이상으로 풂) 아래 어디서 출발해도 신뢰영역법은 ∇f=0인 정류점에 수렴한다. 증명 골자: ρ>¼이면 Δ↑·걸음 수용, ρ<¼이면 Δ↓·거절이라, 모델이 잘 맞는 한 비용이 충분히 감소(코시 감소 보장)하고 ∑감소가 유한 비용에 묶여 ‖∇f‖→0. 단 정류점=국소최소일 수 있음.
R3° 구조 편입: 신뢰영역은 비선형최적화의 황금 표준 안전망이고, 부분문제의 ‖Δθ‖=Δ 처리는 라그랑주 승수(=레벤버그-마카트의 λI). 대수↔해석 사상: 제약 최소화의 KKT 대수(Δθ=−(H+λI)⁻¹g)가 "신뢰반지름 기하"를 "감쇠 뉴턴 한 걸음의 해석"으로 잇고, #22의 모델예측제어에 연결.
R4° 파산(수식): 비볼록 안장점에서 전역최소 미보장 + 부분문제 난해 — 헤시안 H가 음의 고유값(안장점)을 가지면 모델 m이 아래로 무한 발산해 ‖Δθ‖=Δ 경계해를 정확히 푸는 게 별도 어려움이 되고, "전역 수렴"은 정류점 도달일 뿐 전역최소가 아니다. 고차원 비볼록 지형은 안장점이 국소최소보다 지수적으로 많아 빠른 안장 탈출 반경 전략의 보편 최적형 부재.
쉽게: 내 근처만 정확한 지도를 믿는 반경 안에서 한 걸음 정하고, 예측이 맞았으면 반경을 넓히는데, 어디서 출발하든 정류점엔 닿지만 가장 깊은 바닥은 아닐 수 있다.

### ⑤ 새 의문
신뢰영역·뉴턴류는 변수가 한 덩어리로 *얽혀* 있을 때 헤시안 H가 거대해 다루기 벅차다. **차라리 문제를 *여러 조각으로 쪼개*, 각 조각을 따로 풀고 "약속(제약)을 맞춰가며" 합의에 이르게 — 즉 *분산해서* 풀 수 없을까?** → 다음 도구 **ADMM**의 ①로. 닿는 난제: #16, #22.

**[도구 창조 씨앗]** 규칙 "반지름을 *공 모양*(‖Δθ‖₂≤Δ)"을 "*상자 모양*(각 좌표 |Δθⱼ|≤Δ)"으로 뒤집으면? → 상자 신뢰영역, 좌표별 제약 쉬움. 위험 검문소: **②정의** — 상자 안 2차 최소화는 공 안보다 부분문제가 *더 어려운* 별도 최적화라, "쉽게 푼다"는 전제가 흔들린다.

---

> 〔뿌리 줄기〕 **방금 신뢰영역방법으로 "믿을 반지름을 직접 정하고 그 안에서 최선의 걸음을 푸는 틀"을 얻었다.** 그런데 변수가 한 덩어리로 *얽혀* 있으면 헤시안 H가 거대해 벅차다. 여기서 줄기가 "쪼개기"의 다른 갈래로 갈라진다.
> ```
> 신뢰영역(반지름 안 최선)
>  ├─ 줄기①(다음 도구): 문제를 조각으로 쪼개 따로 풀고 약속 맞추기 → ADMM
>  ├─ 곁가지: 스테이하우그-CG로 부분문제 풀기 → 크릴로프(마지막 카드)
>  └─ 곁가지: 거대 최적화·동적평형 → 난제 #16·#22
> ```
> "한 덩어리가 벅차면, *조각으로 나눠* 각자 풀고 라그랑주 승수로 합의시키자" — 다음 카드가 그 분산 푸는 법을 벼린다.

---

─────────────  ▶ 문제 트레이닝: 신뢰영역방법(Trust Region)  ─────────────

【예제】
 예제 1. 1차원 2차 모델 m(p)=f+g·p+½H·p², f=10, g=−4, H=2. 제약 없을 때 m을 최소로 하는 걸음 p는?
   풀이: m'(p)=g+H·p=0 → p=−g/H=−(−4)/2=2. 이유: 포물선 바닥은 1차 미분이 0인 곳. ∴ p=2.
 예제 2. 예제 1에서 신뢰반지름 Δ=1이면(|p|≤1) 걸음 p는?
   풀이: 무제약 최적 p=2가 반지름 1을 넘는다 → 경계로 자른다 → p=1(부호 유지). 이유: 모델을 Δ까지만 믿으므로 그 안 최선은 경계. ∴ p=1.
 예제 3. 신뢰비 ρ=(실제 줄어든 비용)/(모델이 예측한 줄어든 비용). 실제로 3 줄고, 모델은 4 줄 거라 예측했으면 ρ는? ρ>3/4면 반지름을 2배로 키운다 — 키우나?
   풀이: ρ=3/4=0.75. 규칙 "ρ>3/4"는 *초과*라야 키우므로 0.75는 경계(초과 아님) → 유지. 이유: ρ는 예측 정확도 점수. ∴ ρ=0.75, 유지(딱 경계).

【문제】
 ◇기초 1. m(p)=f+g·p+½H·p², g=6, H=3. 무제약 최소 걸음 p=−g/H는? 2. 실제 감소 2, 예측 감소 8. 신뢰비 ρ는?
 ◇기본 3. g=−6, H=2, 무제약 최적 p=−g/H를 구하고, Δ=2면 실제 걸음 p는? 4. ρ=0.1일 때 규칙(ρ<1/4면 Δ←Δ/4). 현재 Δ=4면 새 Δ는?
 ◇유형 5. g=−10, H=5. 무제약 최적 p와, Δ=1·Δ=2·Δ=3에서 실제 걸음 p를 각각 구하라(|p|≤Δ로 자름).
        6. 규칙: ρ>3/4면 Δ←2Δ, ρ<1/4면 Δ←Δ/4, 그 사이는 유지, ρ≤0이면 걸음 거절. Δ=2에서 ρ값 0.9, 0.5, 0.1, −0.2 각각에 대해 다음 Δ(거절이면 그대로)는?
 ◇심화 7. m(p)=5 + (−4)p + ½·2·p². Δ=3. 무제약 최적 p와 그때 모델 예측 감소 m(0)−m(p)를 구하라. (m(0)=5)
        8. 7에서 실제로 가보니 비용이 8에서 5.5로 줄었다(실제 감소 2.5). 예측 감소는 7에서 구한 값. ρ를 구하고, 규칙대로 다음 Δ를 정하라.
 ◇최상위 9. 헤시안이 음의 곡률을 가질 때(안장점): m(p)=f+g·p+½H·p²에서 H=−2(음수), g=0. 무제약으로는 m(p)=f−p²가 *아래로 무한히 발산*한다(바닥이 없다). 신뢰반지름 Δ=3이 *왜 꼭 필요한지*, 그리고 이때 최선의 걸음이 경계 |p|=Δ에 놓이는 이유를 설명하라. 안장점이 비볼록 문제에서 왜 골칫거리인가?

【해설】
 1. p=−g/H=−6/3=−2. 포물선 바닥. 답: p=−2.
 2. ρ=실제/예측=2/8=0.25. 답: ρ=0.25.
 3. p=−(−6)/2=3. Δ=2<3이라 경계로 자름 → p=2(부호 유지). 흔한 실수: 부호 떼고 자르기. 답: 무제약 3, 실제 p=2.
 4. ρ=0.1<1/4 → Δ←Δ/4=4/4=1. 답: 새 Δ=1.
 5. 무제약 p=−(−10)/5=2. Δ=1: |2|>1 → p=1. Δ=2: |2|=2 → p=2(딱 경계, 자를 필요 없음). Δ=3: |2|<3 → p=2(무제약 그대로). 답: 무제약 2; Δ=1→1, Δ=2→2, Δ=3→2.
 6. ρ=0.9>3/4 → Δ←2·2=4. ρ=0.5(1/4와 3/4 사이) → 유지 Δ=2. ρ=0.1<1/4 → Δ←2/4=0.5. ρ=−0.2≤0 → 걸음 거절(그리고 보통 Δ 줄임; 여기선 거절). 답: 4, 2, 0.5, 거절.
 7. 무제약 p=−g/H=−(−4)/2=2. m(2)=5+(−4)(2)+½·2·(2²)=5−8+4=1. 예측 감소=m(0)−m(2)=5−1=4. 답: p=2, 예측 감소=4.
 8. ρ=실제/예측=2.5/4=0.625. 1/4<0.625<3/4 → 규칙상 *유지*. 답: ρ=0.625, Δ 유지.
 9. H=−2<0이면 m(p)=f+0·p+½(−2)p²=f−p²로, p가 커질수록 m이 *무한히 작아져* 바닥(무제약 최소)이 없다 → 걸음이 무한대로 도망친다. 신뢰반지름 Δ=3이 |p|≤3으로 *가둬야만* 부분문제가 답을 가진다. 그 안에서 m=f−p²는 |p|가 클수록 작으니 최선은 *경계* |p|=3(p=3 또는 −3, m=f−9). 안장점은 비볼록 고차원 지형에 *국소최소보다 지수적으로 많아*, 이 음의 곡률 방향마다 모델이 발산해 부분문제가 매번 까다로워지고, 빨리 빠져나갈 반지름 전략의 보편 최적형이 없다 — 그래서 골칫거리다. 답: Δ가 없으면 무한 발산이라 꼭 필요; 최선은 경계 |p|=3; 안장점은 비볼록서 무수히 많아 난제.

【정답】 1) p=−2  2) ρ=0.25  3) 무제약 3, 실제 p=2  4) Δ=1  5) 무제약 2; 1,2,2  6) 4, 2, 0.5, 거절  7) p=2, 예측 감소 4  8) ρ=0.625, 유지  9) Δ로 가둬야 답 존재, 최선은 경계 |p|=3, 안장점은 비볼록서 난제
【발명의 씨앗】 기초~심화까지는 무제약 최적 p=−g/H, 반지름으로 자르기 |p|≤Δ, 신뢰비 ρ=실제/예측, 그리고 ρ로 반지름을 키우고 줄이는 규칙이 모두 산수로 또렷했다 — 레벤버그-마카트의 λ보다 *직접* 믿는 범위를 그어 안전했다. 그런데 최상위 9에서 벽이 터졌다: 헤시안 H가 음의 곡률(안장점)을 가지면 2차 모델 m이 아래로 *무한 발산*해, 신뢰반지름이 없으면 부분문제 자체가 답을 잃고, 최선의 걸음이 경계로 밀려나 정확히 푸는 게 별도 난제가 된다. 이 공식은 "곡률이 양(볼록 골짜기)일 땐 완벽한데, 음의 곡률(안장점)이 끼면" 부분문제와 전역최소 보장이 □무너진다. 그 무너짐이 폭발하는 곳 = 난제 #19(다파라미터 추정)·#22(비선형 동적평형). 이 난제용 도구란, "안장점이 무수한 비볼록 거대 지형을 빠르고 안전히 헤쳐 가는" 이 불편함을 메우는 것 — 다음 카드는 문제를 *쪼개* 그 거대함을 공략한다.

---

## ▣ ADMM / Alternating Direction Method of Multipliers   지구15 · 선행: ← 신뢰영역, 지구07 라그랑주·쌍대성 → 잠금해제: #16, #22

### ① 의문
(신뢰영역 ⑤에서 받음.) 변수 100만 개가 한 덩어리로 얽힌 거대 최적화는 통째로 풀기 벅차다. **문제를 *두 조각*으로 쪼개, 각 조각을 *번갈아* 따로 쉽게 풀고, "두 조각이 서로 약속(같은 값이 되자)을 지키도록" 벌금으로 맞춰가며, 결국 통째 푼 것과 같은 답에 이를 수 없을까?** (= "쪼개고 가속"의 핵심.)

### ② 벽
라그랑주 쌍대(Lagrange duality, 지구07)는 제약을 벌금으로 바꿔주지만, 변수가 한 덩어리로 얽힌 채라 *여전히 통째로* 풀어야 한다. 단순히 벌금만 더한 방법(쌍대 상승)은 함수가 매끈하지 않으면 *수렴이 안 되거나 까다롭다*. "쪼개서 번갈아 풀되 *수렴은 보장*"하는 다리가 없다.

```
   통째 변수 z (100만개) ── 풀기 벅참
        ↓ 쪼갬: x조각 + z조각, 약속 "x=z"
   ┌─ x 풀기(쉬움) ─┐    ┌─ z 풀기(쉬움) ─┐
   │  (z 고정)       │ ↔ │  (x 고정)       │  번갈아!
   └────────────────┘    └────────────────┘
        ↑ 약속 어긴 만큼 벌금 λ(쌍대변수) 올려 맞춤
```

### ③ 발명
문제를 min f(x) + g(z) s.t. Ax + Bz = c 꼴로 쪼갠다(두 조각 x, z와 "약속" 제약). **증강 라그랑지안(augmented Lagrangian)**을 세우고 — 라그랑주 벌금에 *제곱 벌금* (ρ/2)‖Ax+Bz−c‖²를 더 얹어 매끈하게 — x, z를 **번갈아(alternating)** 최소화하고, 약속을 어긴 만큼 **승수(multiplier) λ**를 올린다. 새 기호: 증강 라그랑지안 Lρ, 쌍대변수 λ, 벌금 강도 ρ.

### ④ 해부

**R0 — 중2의 눈.**
두 사람이 큰 일을 나눠 한다. 각자 자기 몫만 잘하면 쉽다. 단 "이음매에서 둘의 답이 같아야 한다"는 약속이 있다. 번갈아 — 한 사람 일하는 동안 다른 사람 결과는 고정 — 자기 몫을 다듬고, 약속을 어긴 만큼 *벌금*을 매겨 둘을 점점 맞춰간다. 반복하면 따로 했는데도 함께 푼 답이 된다.

▲ **R1로 — 이유:** "벌금"과 "번갈아"를 식으로 정확히 적어야 한다.

**R1 — 고교의 손.**
증강 라그랑지안:
$$L_\rho(x,z,\lambda) = f(x) + g(z) + \lambda^\top(Ax+Bz-c) + \tfrac{\rho}{2}\lVert Ax+Bz-c\rVert^2$$
세 갱신(번갈아):
$$x \leftarrow \arg\min_x L_\rho(x,z,\lambda),\quad z \leftarrow \arg\min_z L_\rho(x,z,\lambda),\quad \lambda \leftarrow \lambda + \rho(Ax+Bz-c)$$
한 글자씩:
- **f(x), g(z)**: 두 조각의 비용. x조각·z조각이 각자 책임.
- **Ax + Bz = c**: "약속"(제약). A, B는 변수를 섞는 행렬, c는 목표값. 흔히 x=z 꼴(A=I, B=−I, c=0).
- **λ (람다)**: 못박기 — 여기 λ는 **라그랑주 승수(쌍대변수, dual variable)** = "약속을 어긴 데 매기는 *벌금 가격표*". 고유값도 감쇠인자도 *아니다*. λᵀ(Ax+Bz−c)는 "위반량 × 가격".
- **ρ (로)**: 못박기 — 여기 ρ는 밀도가 아니라 **벌금 강도(penalty parameter)**. (ρ/2)‖Ax+Bz−c‖²는 약속을 어기면 *제곱으로* 더 아프게 하는 항(매끈함을 더해줌).
- **arg min_x**: "L을 가장 작게 만드는 x를 골라라"(그 x값을 반환). x 풀 땐 z, λ 고정.
- **λ ← λ + ρ(Ax+Bz−c)**: "약속을 어긴 양만큼 벌금을 올린다." 위반이 0이 되면 λ가 안정되고 — 그게 수렴.

통째 의미: "x 다듬고 → z 다듬고 → 벌금 올리고를 반복. 따로 풀지만 벌금이 둘을 약속으로 묶어 함께 푼 답으로 수렴."

▲ **R2로 — 이유:** "정말 수렴하는가, 얼마나 빠른가"를 정리로 봐야 한다.

**R2 — 학부의 머리.**
정리: f, g가 *볼록(convex)*이면(골짜기 하나, 매끈함은 불필요) ADMM은 ρ가 무엇이든 *최적값으로 수렴*한다 — 이게 강력함이다. 단 못박기: 수렴은 보장되지만 **속도는 ρ에 매우 민감**해, ρ를 잘못 고르면 *수렴은 하되 거북이처럼 느리다*. (함정: "수렴 보장 ✓"와 "빠름 ✗"이 *따로 논다* — 보장된다고 빠른 게 아니다.) 또 *비볼록*(딥러닝·위상 문제)이면 수렴 보장이 일반적으로 *깨진다* — 잘 되는 사례는 많지만 전역 보장은 부분적.

▲ **R3로 — 이유:** 다른 분해법·쌍대성과의 관계, 어디 쓰는지 봐야 한다.

**R3 — 대학원의 시야.**
ADMM은 "쌍대 상승 + 증강 라그랑지안 + 가우스-자이델 번갈음"의 합성이고, 더글라스-라치포드 분할(Douglas-Rachford splitting)과 동치다. 변수를 여러 기계로 쪼개 *병렬·분산*으로 푸는 #16의 대규모 학습·합의(consensus) 문제, 영상 복원, #22의 분산 제어에 표준이다. 각 x·z 부분문제는 다시 신뢰영역·LM·크릴로프로 푼다.

▲ **R4로 — 이유:** "최적 ρ·비볼록 보장" 부재로.

**R4 — 최전선의 칼날.**
가장 빠른 수렴을 주는 ρ를 *문제 보기 전에* 정하는 일반 처방, 그리고 비볼록 ADMM이 *언제* 좋은 점으로 수렴하는지의 충분조건 — **여기서부터 인류가 다 모른다.** 적응적 ρ 조절은 경험칙이 대부분이다.

**【정밀층 — 집필표준 v2】**
[락] 본 카드에서 λ는 라그랑주 승수(쌍대변수, 위반의 벌금 가격표 — 고유값·감쇠인자 아님), ρ는 벌금 강도(밀도 아님), x·z는 두 조각, L_ρ는 증강 라그랑지안.
R1° 정의역·공역: ADMM은 사상 (x,z,λ) ↦ (x⁺,z⁺,λ⁺), 즉 x←argmin_x L_ρ, z←argmin_z L_ρ, λ←λ+ρ(Ax+Bz−c)를 번갈아 적용 — 쪼갠 두 조각과 쌍대변수를 합의로 수렴시킨다. 글로윈스키·가빈(1970s)이 발명.
R2° 보조정리: Lemma(볼록이면 수렴). f,g가 볼록·하반연속이면 임의 ρ>0에서 ADMM의 원시잔차 ‖Ax+Bz−c‖→0이고 목적값이 최적으로 수렴. 증명 골자: 증강 라그랑지안에 제곱 벌금 (ρ/2)‖·‖²를 더하면 부분문제가 강볼록이 되어 각 단계가 비확장(non-expansive)이고, 더글라스-라치포드 분할의 고정점 반복이 단조 감소하는 리아푸노프 함수를 가져 수렴.
R3° 구조 편입: ADMM은 쌍대 상승+증강 라그랑지안+가우스-자이델 번갈음의 합성이고 더글라스-라치포드 분할과 동치(근접연산자 대수). 대수↔해석 사상: λ 갱신 λ←λ+ρ(위반)이 "라그랑주 쌍대성의 대수"를 "분산·병렬 합의 최적화의 해석"으로 잇고, 각 부분문제는 신뢰영역·LM·크릴로프로 다시 푼다.
R4° 파산(수식): 수렴 보장과 속도가 따로 + 비볼록서 보장 붕괴 — 볼록이라도 수렴 속도가 ρ에 민감해, ρ를 잘못 고르면 잔차 ‖Ax+Bz−c‖이 수렴은 하되 O(1/k)로 거북이처럼 느리다(보장 ✓, 빠름 ✗). 비볼록(딥러닝·위상 문제)이면 부분문제 argmin이 다봉이라 비확장성이 깨지고 더글라스-라치포드 고정점 수렴이 일반적으로 정의 불능 → 전역 보장 상실.
쉽게: 큰 문제를 둘로 쪼개 번갈아 풀고 약속 어긴 만큼 벌금 λ를 올려 합의에 이르는데, 볼록이면 늘 수렴하지만 ρ를 잘못 잡으면 수렴이 끝없이 느려진다.

### ⑤ 새 의문
ADMM·LM·신뢰영역은 모두 *미분(그래디언트 ∇, 자코비안 J, 헤시안 H)*을 먹고 산다. 그런데 모델이 수백 줄 코드로 얽혀 있으면 *손으로 미분하기 불가능*하고, 수치로 떼면 오차가 크다. **복잡한 계산 프로그램의 미분을 *기계가 정확히·자동으로* 뽑아줄 수 없을까?** → 다음 도구 **자동미분**의 ①로. 닿는 난제: #16, #19.

**[도구 창조 씨앗]** 규칙 "두 조각으로 *번갈아*"를 "N조각을 *동시에 병렬*로"로 뒤집으면? → 합의 ADMM(consensus). 위험 검문소: **③무모순** — 셋 이상을 진짜 동시에 갱신하면 ADMM의 수렴 증명(두 블록 가우스-자이델 구조)이 *그대로는 깨져*, 보장이 사라질 수 있다(추가 조건 필요).

---

> 〔뿌리 줄기〕 **방금 ADMM으로 "문제를 조각으로 쪼개 라그랑주 승수로 합의시키는 분산 법"을 얻어, 쪼개기 줄기를 마쳤다.** 그런데 ADMM·LM·신뢰영역은 모두 *미분(∇, J, H)*을 먹고 산다 — 모델이 수백 줄 코드면 손으로 미분이 불가능하다. 여기서 줄기가 "미분을 정확히"로 갈라진다.
> ```
> ADMM(쪼개 합의) ─── 쪼개기 줄기 마감
>  ├─ 줄기①(다음 도구): 복잡한 프로그램의 미분을 기계가 정확히 자동으로 → 자동미분(AD)
>  ├─ 곁가지: 수치미분의 오차 vs 자동미분의 정확성 → 함정 분별
>  └─ 곁가지: 거대 최적화·다파라미터 → 난제 #16·#19
> ```
> "모든 가속·쪼개기가 *미분*을 먹는데, 손미분이 불가능하다 — *기계가 정확히* 뽑게 하자" — 다음 카드가 그 자동 미분기를 벼린다.

---

─────────────  ▶ 문제 트레이닝: ADMM  ─────────────

【예제】
 예제 1. 약속(제약)이 x=z 꼴이다. A=1, B=−1, c=0으로 Ax+Bz−c를 쓰면? x=3, z=2일 때 위반량은?
   풀이: Ax+Bz−c = 1·x + (−1)·z − 0 = x−z. x=3, z=2 → 위반=3−2=1. 이유: 약속 x=z를 어긴 양이 x−z. ∴ x−z=1.
 예제 2. λ 갱신은 λ←λ+ρ(Ax+Bz−c). 현재 λ=0, ρ=1, 위반 x−z=1이면 새 λ는?
   풀이: λ←0+1·(1)=1. 이유: 약속을 어긴 양만큼 벌금(가격표 λ)을 올린다. ∴ λ=1.
 예제 3. 위반이 0이 되면(x=z) λ는 어떻게 되나? 그게 왜 수렴 신호인가?
   풀이: λ←λ+ρ·0=λ(그대로). 위반이 0이라 λ가 더는 안 변하고 *안정*된다 — 두 조각이 약속을 지켜 합의에 이른 것이라 이것이 수렴 신호. ∴ λ 안정=수렴.

【문제】
 ◇기초 1. A=1, B=−1, c=0, x=5, z=5. 위반 Ax+Bz−c는? 2. λ=2, ρ=1, 위반=3. 새 λ=λ+ρ(위반)는?
 ◇기본 3. 증강 라그랑지안의 제곱 벌금 항은 (ρ/2)‖Ax+Bz−c‖². ρ=4, 위반=2(스칼라)면 이 항의 값은? 4. λ=1, ρ=2, 위반 x−z=−0.5. 새 λ는?
 ◇유형 5. 합의 문제 min ½(x−3)²+½(z−1)² s.t. x=z. x-갱신 공식 x=(a−λ+ρz)/(1+ρ)에 a=3, λ=0, ρ=1, z=0 넣어 새 x를 구하라.
        6. 5에서 얻은 x로 z-갱신 z=(b+λ+ρx)/(1+ρ), b=1, λ=0, ρ=1을 풀고, 이어 λ-갱신 λ←λ+ρ(x−z)를 하라.
 ◇심화 7. 5,6의 한 번 갱신 결과(x≈1.5, z≈1.25, λ=0.25)에서 두 번째 x-갱신 x=(a−λ+ρz)/(1+ρ), a=3, λ=0.25, ρ=1, z=1.25를 구하라. 합의값(이 문제의 정답은 평균 (3+1)/2=2)에 가까워지나?
        8. 같은 ρ=1로 계속 돌리면 x,z가 2로, 위반 x−z가 0으로 간다. 만약 ρ를 아주 작게(예 ρ=0.01) 잡으면 수렴은 *되지만* 무엇이 나빠지나? (속도) 한 줄로.
 ◇최상위 9. 정리: f,g가 볼록이면 ADMM은 ρ가 무엇이든 최적값으로 수렴한다. 그런데 (가) ρ를 잘못 고르면 수렴이 거북이처럼 느리고, (나) f나 g가 *비볼록*(예: 딥러닝 손실, 위상 제약)이면 수렴 보장이 일반적으로 깨진다. "수렴 보장 있음"과 "빠름"이 왜 따로 노는지, 그리고 비볼록에서 왜 보장이 사라지는지 설명하라.

【해설】
 1. 1·5+(−1)·5−0=0. 약속 x=z를 지켜 위반 0. 답: 0.
 2. λ=2+1·3=5. 위반만큼 벌금 올림. 답: 5.
 3. (ρ/2)·위반²=(4/2)·2²=2·4=8. 흔한 실수: 제곱 빼먹고 ρ/2·위반=4. 답: 8.
 4. λ=1+2·(−0.5)=1−1=0. 음의 위반이면 λ가 내려갈 수도 있다. 답: 0.
 5. x=(3−0+1·0)/(1+1)=3/2=1.5. 공식 직접 대입(z, λ 고정). 답: x=1.5.
 6. z=(1+0+1·1.5)/(1+1)=2.5/2=1.25. λ←0+1·(1.5−1.25)=0.25. 흔한 실수: z-갱신에 *새* x(1.5)가 아니라 옛 x 쓰기(번갈음은 최신값 사용). 답: z=1.25, λ=0.25.
 7. x=(3−0.25+1·1.25)/(1+1)=(3−0.25+1.25)/2=4/2=2.0. 합의값 2에 *정확히* 닿았다(다음 z·λ가 따라오며 안정). 답: x=2.0, 합의값 2로 수렴 중.
 8. 수렴은 보장되지만 *너무 느려진다* — ρ가 작으면 약속 위반에 매기는 제곱 벌금이 약해 두 조각을 맞추는 힘이 약하고, 잔차 x−z가 0으로 가는 속도가 거북이처럼 느려진다. 답: 수렴은 되나 속도가 매우 느려짐.
 9. (가) 볼록이라 수렴은 *보장*되지만, 그 속도는 ρ에 민감해 ρ를 잘못 잡으면 잔차가 O(1/k)로 한없이 느리게 0으로 간다 — "보장 ✓"와 "빠름 ✗"이 별개다. (나) 비볼록이면 x·z 부분문제의 argmin이 여러 골짜기를 가져(다봉) 각 갱신이 더는 *비확장(non-expansive)*이 아니고, ADMM의 수렴을 떠받치던 더글라스-라치포드 고정점 구조가 깨져 전역 수렴 증명이 일반적으로 성립하지 않는다(잘 되는 사례는 많아도 보장은 부분적). 답: 보장과 속도는 별개(ρ 민감), 비볼록이면 비확장성·고정점 구조가 깨져 보장 상실.

【정답】 1) 0  2) 5  3) 8  4) 0  5) x=1.5  6) z=1.25, λ=0.25  7) x=2.0(합의값 2로)  8) 수렴되나 매우 느림  9) 보장≠빠름(ρ 민감), 비볼록서 보장 상실
【발명의 씨앗】 기초~심화까지는 위반량 Ax+Bz−c, 제곱 벌금 (ρ/2)‖·‖², 세 갱신(x→z→λ)을 번갈아 돌리니 따로 푼 두 조각이 합의값 2로 *정확히* 모여드는 게 신기할 만큼 깔끔했다 — 거대 문제를 쪼개 푸는 힘을 얻었다. 그런데 심화 8과 최상위 9에서 벽이 터졌다: 볼록이라 수렴은 보장돼도 ρ를 잘못 고르면 거북이처럼 느리고("보장"과 "빠름"이 따로 논다), 비볼록이면 부분문제가 다봉이 되어 수렴 보장 자체가 □무너진다. 이 공식은 "볼록·좋은 ρ일 땐 완벽한데, 비볼록이거나 ρ가 나쁘면" 빠른 합의가 무너진다. 그 무너짐이 폭발하는 곳 = 난제 #16(거대 최적화)·#22(비선형 동적평형 제어). 이 난제용 도구란, "비볼록에서도 빠르게 합의로 수렴하는 ρ와 분할"이라는 이 불편함을 메우는 것. 그리고 이 모든 갱신이 미분 ∇를 먹는데 — 손미분이 불가능한 거대 모델에서 그 ∇를 어떻게 얻나? 다음 카드가 답한다.

---

## ▣ 자동미분 / Automatic Differentiation (AD)   지구15 · 선행: 지구07 미분·연쇄법칙, ← ADMM → 잠금해제: #16, #19

### ① 의문
(ADMM ⑤에서 받음.) 모든 최적화 도구가 그래디언트·자코비안을 먹는다. **수백 줄로 얽힌 계산 프로그램이 있을 때, 그 출력의 미분을 *손 계산 없이, 수치 오차 없이, 기계가 정확히* 뽑아줄 수 없을까?**

### ② 벽
미분을 얻는 기존 두 길이 다 무너진다. (가) **손 미분**: 함수가 복잡하면 사람이 펜으로 풀기 *불가능*하고 틀린다. (나) **수치 미분**(유한차분, (f(x+h)−f(x))/h): h를 작게 하면 *반올림 오차*가 폭발하고, 크게 하면 *근사 오차*가 커진다 — 정확도와 안정의 *함정에 끼인다*. 또 변수가 n개면 n번 함수를 다시 계산해 *느리다*.

```
   수치미분 오차:  h 큼 ← 근사오차 |  반올림오차 → h 작음
        오차 │ ╲                    ╱
             │  ╲__________________╱   ← 최적 h가 좁고, 그래도 오차 남음
             └──────────────────────▶ h
   자동미분:  연쇄법칙을 기계가 *정확히* 적용 → 오차 ≈ 기계정밀도(근사 아님)
```

### ③ 발명
프로그램을 "+, ×, sin, exp 같은 *기본 연산의 사슬*"로 본다. 각 기본 연산의 미분은 *정확히* 안다(예: sin의 미분은 cos). 그럼 연쇄법칙(chain rule)을 기계가 *연산 사슬을 따라* 정확히 곱해가면, 전체 미분이 *근사 없이* 나온다. 두 방식: **전진모드(forward)** = 입력→출력으로 미분을 같이 끌고 감, **역전파모드(reverse)** = 출력→입력으로 미분을 되짚음. 새 기호: 듀얼수(dual number) **a + b·ε** (ε²=0), 수반(adjoint) **x̄ = ∂(출력)/∂x**.

### ④ 해부

**R0 — 중2의 눈.**
복잡한 계산을 *작은 단계들*로 쪼갠다(곱하기, 더하기, 제곱, sin...). 각 작은 단계가 "입력이 조금 변하면 출력이 몇 배 변하나"는 *정확히 안다*. 이 배율들을 단계 순서대로 *곱해 나가면*, 처음 입력이 조금 변할 때 최종 출력이 몇 배 변하는지(=미분)가 *정확히* 나온다. 근사가 아니라 *조립*이다.

▲ **R1로 — 이유:** "배율을 곱해 나간다"를 식·기호로 적어야 한다.

**R1 — 고교의 손.**
연쇄법칙: y = f(g(x))면 **dy/dx = f′(g(x))·g′(x)** — "바깥 미분 × 안쪽 미분". 자동미분은 이를 *모든 단계에* 기계적으로 적용한다.
- **전진모드(듀얼수)**: 보통 수 x를 **x + 1·ε**로 들고 다닌다. 여기 **ε(엡실론)**은 "아주 작은 가상의 양"인데 규칙이 *ε²=0*. 그럼 f(x+ε) = f(x) + f′(x)·ε가 *정확히* 되어, ε 앞 계수가 곧 미분! (ε²=0이 고차항을 *정확히* 잘라준다.)
- **dy/dx**: x가 1만큼 변할 때 y가 변하는 비율(미분).
- **f′(·)**: f의 도함수(미분). 작은따옴표 ′가 "미분했다"는 표시.

▲ **R2로 — 이유:** "변수가 많을 땐 전진이 느리다 → 역전파"의 *비용 구조*를 봐야 한다.

**R2 — 학부의 머리.**
입력 n개, 출력 1개(최적화의 비용 함수가 딱 이 꼴)일 때:
- **전진모드**: 입력 하나당 한 번 훑어 그 입력의 미분을 얻음 → n번 훑어야 함(n 크면 비쌈).
- **역전파모드(reverse, 수반법)**: 먼저 정방향 한 번 계산하며 모든 중간값 저장, 그다음 *출력에서 입력으로* 거꾸로 한 번 훑으며 **수반 x̄ = ∂y/∂x**를 누적 → **단 한 번의 역방향 훑기로 *모든* n개 입력의 미분을 동시에** 얻는다. 이게 딥러닝 역전파(backpropagation)의 정체다.
- **x̄ (엑스바)**: "출력이 이 중간값에 얼마나 민감한가". 위 막대 ‾는 "수반(adjoint)"이라는 뜻.

핵심: 자동미분은 *근사가 아니라* 연쇄법칙의 정확한 기계 적용이라, 오차가 *기계 정밀도* 수준(수치미분의 함정을 *피한다*). 단 함정 못박기: 역전파는 *빠르지만* 모든 중간값을 저장해 **메모리를 많이 먹는다** — "빠름"의 대가가 "메모리 폭증"이다(빠름이 공짜가 아니다).

▲ **R3로 — 이유:** 다른 도구·미분불가 지점과의 연결을 봐야 한다.

**R3 — 대학원의 시야.**
자동미분은 비선형최소제곱의 J, 신뢰영역의 H, ADMM의 ∇를 *공급하는 발전소*다 — 이 지구의 미분 기반 도구 전부가 AD 위에 선다. 역전파모드는 신경망 학습(#16)의 핵심, 전진모드는 출력이 많고 입력이 적을 때 유리. 미분가능 프로그래밍(differentiable programming)·물리 시뮬레이터 역설계로 뻗는다.

▲ **R4로 — 이유:** "미분이 정의 안 되는 연산"의 처리 부재로.

**R4 — 최전선의 칼날.**
프로그램에 *if 분기·정렬·미분 불가능 연산*(절댓값의 0, 이산 선택)이 섞이면, 자동미분이 *무엇을 미분으로 내놔야 하는지*가 모호하다 — 서브그래디언트·평활화·확률적 근사 등 임시방편이 있으나, "이산·불연속을 포함한 프로그램의 *올바른* 자동미분"의 일반 이론은 — **여기서부터 인류가 다 모른다.** 활발한 미해결 영역이다.

**【정밀층 — 집필표준 v2】**
[락] 본 카드에서 ε은 듀얼수의 무한소(ε²=0), x̄는 수반(adjoint, ∂출력/∂x), ′는 미분, ∂는 편미분. λ·σ는 안 쓴다.
R1° 정의역·공역: 자동미분은 사상 (프로그램 f, 입력 x) ↦ (f(x), Jf(x)) — 기본 연산 사슬로 표현된 함수와 입력을 받아 값과 정확한 미분을 함께 돌려준다. 전진모드는 듀얼수 x+1·ε, 역전파모드는 수반 x̄ 누적. 베이딘·린나인마(역전파)가 정립.
R2° 보조정리: Lemma(듀얼수가 정확한 1차 미분). ε²=0인 확장수에서 임의 매끈 f에 대해 f(x+ε)=f(x)+f′(x)ε. 증명: 테일러 전개 f(x+ε)=f(x)+f′(x)ε+½f″(x)ε²+… 인데 ε²=0이라 2차 이상 항이 정확히 0으로 잘려 ε 앞 계수가 곧 f′(x). 합성도 (g∘f)(x+ε)=g(f(x))+g′(f(x))f′(x)ε로 연쇄법칙이 자동 — 근사 없는 대수적 항등.
R3° 구조 편입: 듀얼수는 ℝ[ε]/(ε²)라는 가환환(거듭멱영 원소 ε)이고, 역전파는 연쇄법칙=자코비안 곱(범주의 사상 합성)의 역방향 누적. 대수↔해석 사상: 듀얼수 환의 곱 규칙이 "미분의 라이프니츠 대수"를 "프로그램 미분의 기계적 해석"으로 잇고, 비선형최소제곱 J·신뢰영역 H·ADMM ∇를 공급.
R4° 파산(수식): 미분 불가능·이산 연산에서 정의 불능 — if 분기·정렬·절댓값의 꺾임(x=0에서 d|x|/dx 미정)·이산 선택이 섞이면 연쇄법칙이 적용할 도함수가 그 점에 존재하지 않아 자동미분이 "무엇을 내놓을지"가 모호하다(서브그래디언트 집합 [−1,1] 중 택일). 또 역전파는 모든 중간값을 저장해 메모리 ~ 연산 길이로 폭증(빠름의 대가).
쉽게: 복잡한 계산을 작은 단계로 쪼개 각 단계의 정확한 배율을 곱해 미분을 근사 없이 뽑는데, if·절댓값처럼 꺾인 곳에선 무엇을 미분으로 내놓을지 애매해진다.

### ⑤ 새 의문
미분으로 *작은 한 걸음*은 잘 정했다. 그런데 어떤 시스템은 "지금 상태를 *연속적으로* 시간에 따라 굴리는" 답이 필요하다 — 예: dx/dt = Ax의 해는 x(t)=exp(At)x(0)처럼 *행렬의 지수함수*다. **행렬을 지수함수에 넣는다는 게 대체 뭐고, 거대한 행렬에서 그걸 어떻게 계산하나?** → 다음 도구 **행렬지수함수**의 ①로. 닿는 난제: #22.

**[도구 창조 씨앗]** 규칙 "미분(1차 변화율)만 자동 추출"을 "2차(헤시안)까지 자동 추출"로 뒤집으면? → 고차 AD(테일러 모드). 위험 검문소: **⑤정직** — 2차 미분은 항이 n² 개로 *폭발*해, "공짜로 정확히"라는 AD의 장점이 비용 면에서 *진짜 장벽*에 부딪힌다(왜 아직 거대 2차 AD가 흔치 않은가의 정직한 이유).

---

> 〔뿌리 줄기〕 **방금 자동미분으로 "복잡한 프로그램의 미분을 기계가 정확히 뽑는 도구"를 얻었다.** 미분으로 *작은 한 걸음*은 똑똑히 정했다. 그런데 어떤 시스템은 상태를 *연속적으로 시간에 따라 굴리는* 답(dx/dt=Ax → x(t)=exp(At)x₀)이 필요하다. 여기서 줄기가 "거대 계산 가속"으로 갈라진다.
> ```
> 자동미분(정확한 한 걸음)
>  ├─ 줄기①(다음 도구): 상태를 시간에 따라 굴리는 행렬의 지수함수 → 행렬지수함수
>  ├─ 곁가지: 미분의 역방향 누적 = 역전파(딥러닝의 심장)
>  └─ 곁가지: 비선형 동적평형 제어 → 난제 #22
> ```
> "한 걸음을 정확히 정했으니, 이제 *연속 시간 흐름* 전체를 한 번에 굴리는 도구로" — 다음 카드가 행렬을 지수에 넣는 법을 벼린다.

---

## ▣ 행렬지수함수 / Matrix Exponential   지구15 · 선행: 지구01 고유값·대각화, ← 자동미분 → 잠금해제: #22

### ① 의문
(자동미분 ⑤에서 받음.) 미분방정식 dx/dt = Ax는 "변화율이 지금 상태에 행렬 A로 비례"하는 시스템이다(흔들리는 다리, 회로, 양자상태의 시간 진행). 그 답이 **x(t) = exp(At)·x(0)** 라는데 — **숫자가 아니라 *행렬* A를 지수함수 exp에 넣는다는 게 대체 무슨 뜻이고, 어떻게 계산하나?**

### ② 벽
보통 지수함수 eˣ는 *숫자* x를 받는다. 행렬 A를 e^A의 지수로 넣으려면, "행렬을 거듭제곱하고 무한히 더한다"는 새 의미가 필요한데 — 그게 *수렴하는지*, *어떻게 실제로 계산하는지*가 막막하다. 단순히 각 칸에 eˣ를 따로 씌우는 건 *틀린 답*이다(연결이 끊김).

```
   잘못:  A=[[a,b],[c,d]] → 각 칸에 e씌움 [[eᵃ,eᵇ],...]  ✗ (의미 없음)
   맞음:  exp(A) = I + A + A²/2! + A³/3! + ...  (행렬 거듭제곱의 무한합)
          또는 A=PΛP⁻¹(대각화)면 exp(A)=P·exp(Λ)·P⁻¹  (고유값마다 eᵏ)
```

### ③ 발명
숫자 지수의 무한급수 eˣ = 1 + x + x²/2! + …를 *그대로 행렬로* 옮긴다: **exp(A) = I + A + A²/2! + A³/3! + …**. 행렬 거듭제곱 A²·A³…는 행렬 곱셈으로 정의되니 말이 된다. 핵심: A를 고유값으로 대각화(A=PΛP⁻¹)하면 exp(A) = P·exp(Λ)·P⁻¹이고, exp(Λ)는 *고유값마다* e^λ를 넣은 대각행렬 — 어려운 행렬 지수가 *고유값들의 보통 지수*로 풀린다.

### ④ 해부

**R0 — 중2의 눈.**

   [중2 시각] 행렬지수 exp(At) — 늘고/도는 모드가 얽혀 함께 자람
```
   고유값 실수부 > 0 (불어나는 모드):   ●→──●→───●→────  쑥쑥 커짐
   고유값 실수부 < 0 (사그라드는 모드):  ●→──●→──●→─●→·   잦아듦
   고유값 허수부 (도는 모드):           ●↻  ●↺  ●↻   빙빙 진동
        exp(At) = 이 세 거동을 한 행렬에 담아 시간 t만큼 한 번에 굴림
```
   숫자 지수 eᵗ가 한 방향 성장이라면, 행렬 지수는 여러 방향이 얽힌 채 어떤 건 불어나고(실수부 +) 어떤 건 빙빙 돈다(허수부). 시스템을 시간 t만큼 한 번에 굴린 결과다.
   Where eᵗ grows one way, exp(At) bundles many tangled directions — some grow, some rotate — rolling the system forward by time t at once.

보통 지수함수 eᵗ는 "꾸준히 t배로 불어나는 성장"이다. 행렬 지수 exp(At)는 "여러 방향이 *서로 얽힌 채* 동시에 자라고 도는 성장"이다 — 어떤 방향은 늘고(불어나는 모드), 어떤 방향은 빙빙 돈다(진동 모드). 시스템을 시간 t만큼 *한 번에* 굴린 결과를 담은 행렬.

▲ **R1로 — 이유:** "행렬을 지수에 넣는다"를 식으로 정의해야 한다.

**R1 — 고교의 손.**
$$\exp(A) = \sum_{k=0}^{\infty}\frac{A^k}{k!} = I + A + \frac{A^2}{2!} + \frac{A^3}{3!} + \cdots$$
한 글자씩:
- **exp(A)** (또는 e^A): 행렬 A를 넣은 지수함수. 결과도 *행렬*.
- **Σ_{k=0}^∞**: "k를 0부터 무한대까지 다 더하라".
- **Aᵏ**: A를 k번 곱한 행렬(A⁰=I).
- **k!**: k 팩토리얼 = 1·2·…·k. 항을 빠르게 작게 만들어 *무한합이 수렴*하게 한다.
- **I**: 단위행렬(아무것도 안 함). k=0 항.

통째 의미: "행렬 거듭제곱을 팩토리얼로 나눠 무한히 더한다. 각 항이 빠르게 작아져 *항상 수렴*한다(어떤 A든)." 미분방정식 dx/dt=Ax의 해가 x(t)=exp(At)x(0)인 이유: exp(At)를 t로 미분하면 정확히 A·exp(At)가 나오기 때문(급수를 항별 미분하면 보임).

▲ **R2로 — 이유:** "대각화로 어떻게 쉬워지나, 안 되면 어쩌나"를 봐야 한다.

**R2 — 학부의 머리.**
A를 대각화할 수 있으면(A = PΛP⁻¹, P는 고유벡터 모음, Λ는 고유값 대각행렬):
$$\exp(A) = P\,\exp(\Lambda)\,P^{-1}, \quad \exp(\Lambda)=\mathrm{diag}(e^{\lambda_1},\dots,e^{\lambda_n})$$
- **Λ (람다 대문자)**: 못박기 — 여기 Λ는 *고유값들*을 대각선에 늘어놓은 행렬. λ는 **고유값(eigenvalue)**(지구01) — 감쇠인자도 승수도 아니다.
- 통째 의미: "어려운 exp(A)가, 고유값마다 *보통 지수* e^λ를 넣는 일로 분해된다." 고유값 λ의 실수부가 양수면 그 방향은 *폭발 성장*, 음수면 *감쇠*, 허수부는 *진동* — 시스템 안정성이 고유값에 다 적혀 있다.

함정 못박기: 행렬이 *대각화 안 되면*(결손, defective) 이 깔끔한 공식이 무너지고 요르단 형(Jordan form)이 필요하다. 또 실제 계산에서 무한급수를 그냥 더하는 건 *위험* — A의 노름이 크면 큰 항들이 서로 빼지며 *반올림 오차가 폭발*한다("Nineteen Dubious Ways" 문제). (함정: 정의는 간단해도, *빠르고 정확한 계산*은 별개의 어려움.)

▲ **R3로 — 이유:** 실제 계산법과 다른 도구 연결을 봐야 한다.

**R3 — 대학원의 시야.**
실전 계산은 스케일링-제곱(scaling and squaring) + 파데 근사(Padé), 또는 큰 행렬엔 크릴로프(10번 카드)로 exp(A)v만 직접 구한다(전체 exp(A) 없이). 행렬지수는 #22(선형 동역학·제어)의 상태천이행렬, 양자의 시간진화 e^{−iHt}(다음 지구로 이어짐), 마르코프 연속시간 전이의 심장이다.

▲ **R4로 — 이유:** "거대·악조건 행렬의 exp"의 한계로.

**R4 — 최전선의 칼날.**
A가 *비정규(non-normal)*이고 거대하면, 어떤 계산법도 정확도와 비용을 동시에 보장하기 어렵다 — "행렬 함수 f(A)를 큰 비정규 A에서 *안정·고속*으로 계산하는 보편 최적법"은 — **여기서부터 인류가 다 모른다.** 조건수가 나쁜 행렬에서의 오차 한계는 부분적으로만 안다.

**【정밀층 — 집필표준 v2】**
[락] 본 카드에서 Λ는 고유값 대각행렬이고 λ는 고유값(eigenvalue)이지 감쇠인자·라그랑주 승수가 아니다. A는 행렬, I는 단위행렬, k!은 계승.
R1° 정의역·공역: 행렬지수는 사상 exp : ℝ^{n×n} → GL(n), A ↦ ∑ₖ Aᵏ/k! — 행렬을 가역행렬로 보낸다. 미분방정식 dx/dt=Ax의 해 x(t)=exp(At)x₀를 준다. 리 군론·동역학에서 발명.
R2° 보조정리: Lemma(급수가 항상 수렴·해를 줌). ∑ₖ Aᵏ/k!는 임의 A에서 절대수렴하고 d/dt exp(At)=A exp(At). 증명: 노름 부등식 ‖Aᵏ/k!‖ ≤ ‖A‖ᵏ/k! 라 ∑은 스칼라 e^{‖A‖}로 지배되어 수렴(완비 공간서 절대수렴⟹수렴). 항별 미분 ∑k(At)^{k−1}A/k! = A∑(At)^{k−1}/(k−1)! = A exp(At) ⟹ x(t)=exp(At)x₀가 해.
R3° 구조 편입: exp는 리 대수(gl(n))를 리 군(GL(n))으로 보내는 지수사상이고, 대각화 A=PΛP⁻¹이면 exp(A)=P exp(Λ)P⁻¹ (고유값마다 e^λ). 대수↔해석 사상: 이 대각화가 "고유값 스펙트럼의 대수"를 "연속 시간 흐름의 해석"으로 잇고(λ 실수부=성장/감쇠, 허수부=진동), 양자 e^{−iHt}·마르코프 전이로 연결.
R4° 파산(수식): 결손·비정규 행렬에서 깔끔한 공식 붕괴 + 급수 계산 불안정 — A가 대각화 불가(결손, 고유벡터 부족)이면 exp(A)=P exp(Λ)P⁻¹가 정의 불능이라 요르단 형이 필요하고, ‖A‖이 크면 ∑ Aᵏ/k!의 큰 항들이 서로 빼지며 반올림 오차가 폭발한다("Nineteen Dubious Ways"). 또 exp(A+B)=exp(A)exp(B)는 [A,B]≠0이면 거짓(비가환) → 숫자 지수법칙이 파산.
쉽게: 숫자 지수 eᵗ가 한 방향 성장이라면 행렬 지수는 여러 방향이 얽혀 늘고 도는 흐름인데, 정의는 쉬워도 거대·악조건 행렬에선 빠르고 정확한 계산이 따로 어렵다.

### ⑤ 새 의문
exp(A) 같은 *함수를 행렬에 적용*하려면 결국 "함수를 다항식으로 잘 흉내" 내야 한다. 그런데 다항식으로 함수를 흉내 낼 때, *어느 다항식*이 가장 적은 차수로 *가장 골고루* 잘 맞출까? **주어진 함수를, 최악의 오차가 가장 작도록 다항식으로 근사하는 *최선의* 방법이 있을까?** → 다음 도구 **체비쇼프근사**의 ①로. 닿는 난제: #22.

**[도구 창조 씨앗]** 규칙 "exp(*A*) — 한 행렬의 함수"를 "exp(*A+B*) = exp(A)exp(B)?"로 (숫자처럼) 뒤집으면? → 거짓이다(A,B가 안 바뀌면 ≠). 위험 검문소: **③무모순** — 행렬은 곱 순서가 중요(비가환)해, 숫자 지수법칙이 *증명된 사실과 충돌*한다(베이커-캠벨-하우스도르프 보정 필요).

---

> 〔뿌리 줄기〕 **방금 행렬지수함수로 "상태를 연속 시간에 따라 굴리는 exp(At)"를 얻었다.** 그런데 exp(A)처럼 *함수를 행렬에 적용*하려면 결국 그 함수를 *다항식으로 잘 흉내* 내야 한다. 여기서 갈래가 갈라진다.
> ```
> 행렬지수함수(함수를 행렬에)
>  ├─ 줄기①(다음 도구): 최악 오차가 가장 작은 최선의 다항식 흉내 → 체비쇼프근사
>  ├─ 곁가지: exp(A) 직접 계산의 위험("19가지 의심스러운 방법")
>  └─ 곁가지: 대규모 동역학 제어 → 난제 #22
> ```
> "함수를 행렬에 넣으려면 *다항식 흉내*가 필요한데, *어느* 다항식이 가장 골고루 잘 맞나" — 다음 카드가 그 최선의 근사를 벼린다.

---

## ▣ 체비쇼프근사 / Chebyshev Approximation   지구15 · 선행: 지구07 다항식근사, ← 행렬지수 → 잠금해제: #22

### ① 의문
(행렬지수 ⑤에서 받음.) 함수(예: exp, 1/x, √x)를 다항식으로 흉내 내면 계산이 쉬워진다. 그런데 테일러 다항식은 *한 점 근처만* 정확하고 멀어지면 빗나간다. **구간 *전체에서* 최악의 오차가 가장 작도록, 가장 적은 차수로 함수를 다항식으로 *골고루* 근사할 수 없을까?**

### ② 벽
테일러 급수(Taylor series)는 한 점에서의 미분으로 만들어, *그 점 근처는 매우 정확*하지만 *구간 끝으로 가면 오차가 한쪽으로 쏠려 폭발*한다 — "전체에서 골고루"가 아니다. 또 점들을 균등 간격으로 잡아 보간하면, 차수를 높일수록 *구간 끝에서 마구 출렁*인다(룽게 현상, Runge phenomenon).

```
   테일러(중심 0):  ──정확──╲___ 끝에서 오차 폭발
   균등보간(고차):  ∿∿∿‾‾‾∿∿∿  끝에서 출렁(룽게 현상)
   체비쇼프:        ≈≈≈≈≈≈≈≈≈  오차가 구간 전체에 *고르게* 작게 퍼짐(등진동)
```

### ③ 발명
오차가 *한 점에 쏠리지 않고* 구간 전체에 **고르게 퍼지게** 만든다. 핵심: **체비쇼프 다항식 Tₙ(x) = cos(n·arccos x)** 를 기저로 쓰고, 보간점을 *균등이 아니라* "구간 끝에 촘촘한" **체비쇼프 점**으로 잡는다. 그러면 근사 오차가 *위아래로 같은 크기로 진동*(등진동, equioscillation)해 *최악 오차가 최소*가 된다. 새 기호: 체비쇼프 다항식 **Tₙ**, 무한대 노름 **‖·‖∞**.

### ④ 해부

**R0 — 중2의 눈.**
함수를 다항식으로 흉내 내되, "어느 한 곳도 너무 많이 틀리지 않게" *오차를 구간 전체에 골고루 펴는* 게 목표다. 가장 크게 틀린 곳의 오차(최악 오차)를 가능한 한 작게 — 산봉우리들을 다 똑같은 낮은 높이로 깎는 셈. 그러려면 흉내 낼 점들을 *가운데보다 양 끝에 더 촘촘히* 찍어야 끝이 안 출렁인다.

▲ **R1로 — 이유:** "골고루"와 "체비쇼프 점·다항식"을 식으로 적어야 한다.

**R1 — 고교의 손.**
체비쇼프 다항식:
$$T_n(x) = \cos\!\big(n\,\arccos x\big), \quad x\in[-1,1]$$
한 글자씩:
- **Tₙ(x)**: n차 체비쇼프 다항식. T₀=1, T₁=x, T₂=2x²−1, … (점화식 Tₙ₊₁=2x·Tₙ−Tₙ₋₁).
- **cos, arccos**: 코사인과 그 역함수. arccos x는 "코사인이 x가 되는 각도".
- **cos(n·arccos x)**: x를 각도로 바꿔(arccos), n배 한 뒤 다시 코사인 — 이 묘한 조합이 *깔끔한 다항식*이 된다(겉보기와 달리 삼각함수가 사라짐).

근사 품질의 자:
$$\lVert f - p\rVert_\infty = \max_{x\in[-1,1]} \lvert f(x)-p(x)\rvert$$
- **‖f−p‖∞**: "무한대 노름". 함수 f와 다항식 p의 차이 중 *가장 큰 값*(최악 오차). ∞는 "최댓값을 본다"는 뜻. 이걸 *최소화*하는 p가 목표.

통째 의미: "최악 오차 ‖f−p‖∞를 가장 작게 하는 다항식을 찾되, 체비쇼프 기저·점을 쓰면 거의 그 최선에 닿는다."

▲ **R2로 — 이유:** "왜 체비쇼프가 최선에 가까운가"를 정리로 봐야 한다.

**R2 — 학부의 머리.**
**등진동 정리(equioscillation, 체비셰프)**: 구간에서 f를 n차 다항식으로 *최악 오차 최소* 근사하면, 오차 f−p가 *같은 크기로 부호 바꿔가며 적어도 n+2번 진동*한다. 체비쇼프 점에서의 보간은 이 최적에 *지수적으로 가깝게* 수렴한다(f가 매끈하면). 핵심: 균등 점은 룽게 현상으로 *발산*할 수 있지만, 체비쇼프 점은 끝에 촘촘해 그 출렁임을 *상쇄*한다 — Tₙ이 [−1,1]에서 최댓값 1을 *최소한으로* 유지하는 다항식이기 때문. 함정 못박기: 이 모든 좋은 성질은 함수가 *매끈할 때*다. f에 *꺾임·불연속*이 있으면 수렴이 느려지고(깁스 현상 비슷), "골고루 작은 오차"가 *깨진다*.

▲ **R3로 — 이유:** 행렬·다른 도구로의 연결을 봐야 한다.

**R3 — 대학원의 시야.**
체비쇼프 근사는 행렬함수 f(A)를 *다항식 p(A)*로 바꿔 — 그리고 p(A)v를 크릴로프(10번 카드)로 — 거대 행렬의 exp(A)v, √A 등을 푸는 엔진이다. 클렌쇼-커티스 적분, 스펙트럴 방법(미분방정식 풀이), 함수의 빠른 평가에 두루 쓰며 #22의 동역학 계산을 가속한다.

▲ **R4로 — 이유:** "다변수·비매끈 함수의 최적근사" 한계로.

**R4 — 최전선의 칼날.**
변수가 여럿인 함수(고차원)의 최악오차-최소 다항근사는 *차원의 저주*로 항 수가 폭발하고, 그걸 효율적으로 *최적* 근사하는 일반법은 — **여기서부터 인류가 다 모른다.** 불연속·특이점이 섞인 함수의 최적 근사 이론도 부분적이다.

**【정밀층 — 집필표준 v2】**
[락] 본 카드에서 Tₙ은 체비쇼프 다항식, ‖·‖∞는 무한대 노름(최악 오차의 자), x∈[−1,1]은 근사 구간. λ·σ는 안 쓴다.
R1° 정의역·공역: 체비쇼프근사는 사상 f ↦ p* = argmin_{deg p≤n} ‖f−p‖∞ — 함수를 "최악 오차 최소" 다항식으로 보낸다. 기저 Tₙ(x)=cos(n·arccos x), 보간점은 끝에 촘촘한 체비쇼프 점. 체비셰프(1850s)가 발명.
R2° 보조정리: Lemma(등진동 정리). f의 최선 n차 근사 p*는 오차 f−p*가 같은 크기로 부호를 바꿔가며 적어도 n+2번 진동한다(그것과 동치). 귀류법: 만약 진동이 n+1번 이하이면, 그 부호 패턴을 따라 n차 다항식 q를 더해 오차의 봉우리를 한쪽으로 깎을 수 있어 ‖f−(p*+q)‖∞ < ‖f−p*‖∞ — p*가 최선이라는 가정과 모순. 따라서 n+2번 등진동이 최선의 지문.
R3° 구조 편입: Tₙ은 가중치 1/√(1−x²)로 직교하는 직교다항식족(스투름-리우빌의 한 예)이고, [−1,1]에서 최댓값 1을 최소로 유지하는 극값 다항식. 대수↔해석 사상: Tₙ(cosθ)=cos(nθ)가 "삼각 다항식의 대수"를 "최소최대 근사의 해석"으로 잇고, 행렬함수 f(A)≈p(A)v로 크릴로프(가속 엔진)에 연결.
R4° 파산(수식): 비매끈 함수에서 등진동 최적성 붕괴 — f에 꺾임·불연속이 있으면 다항 근사 오차가 그 점 근처에서 깁스류 진동으로 ‖f−p‖∞가 차수 n을 올려도 0으로 안 줄어(불연속점에서 오차 ~ 상수). 또 균등 보간점은 룽게 현상으로 ‖f−p_n‖∞ → ∞로 발산(끝에서 폭주). 고차원 함수는 항 수가 차원의 저주로 폭발 → 최악오차-최소 근사가 정의 불능에 가까워진다.
쉽게: 함수를 다항식으로 흉내 내되 오차를 구간 전체에 골고루 펴 가장 큰 오차를 최소로 깎는데, 함수가 꺾이거나 점을 균등하게 찍으면 끝에서 마구 출렁여 무너진다.

### ⑤ 새 의문
체비쇼프는 f(A)를 다항식 p(A)로 바꾸지만, 행렬 A가 *수백만 차원*이면 p(A) 전체를 만드는 것조차 불가능하다 — 우리가 진짜 원하는 건 보통 **f(A)를 어떤 벡터 v에 적용한 결과 f(A)·v** 하나다. **거대한 행렬을 통째로 다루지 않고, "A를 v에 거듭 곱한 흔적"만으로 답이 사는 *작은 부분공간*을 찾아 거기서 풀 수 없을까?** → 다음 도구 **크릴로프부분공간**의 ①로. 닿는 난제: #16, #22.

**[도구 창조 씨앗]** 규칙 "최악 오차(∞-노름)를 최소화"를 "평균제곱 오차(2-노름)를 최소화"로 뒤집으면? → 르장드르 근사(최소제곱). 위험 검문소: **④표적** — 2-노름 최소화는 *평균*은 좋게 하지만 *최악 한 점*은 더 나빠질 수 있어, "어디서도 크게 안 틀린다"는 원래 표적을 *놓친다*.

---

> 〔뿌리 줄기〕 **방금 체비쇼프근사로 "최악 오차가 가장 작은 최선의 다항식 흉내"를 얻었다.** 그런데 행렬 A가 *수백만 차원*이면 p(A) 전체를 만드는 것조차 불가능하다 — 우리가 진짜 원하는 건 보통 f(A)·v 한 개다. 여기서 이 지구의 마지막 갈래가 갈라진다.
> ```
> 체비쇼프근사(최선의 다항식)
>  ├─ 줄기①(다음 도구, 이 지구 마지막): A를 v에 거듭 곱한 흔적만으로 작은 부분공간에서 풀기 → 크릴로프부분공간
>  ├─ 곁가지: 이 지구의 가속 엔진(LM·신뢰영역·exp·체비쇼프)을 모두 통합
>  └─ 곁가지: 거대 선형계·대규모 동역학 → 난제 #16·#22
> ```
> "거대한 행렬을 통째로 다루지 말고, *답이 사는 작은 방*만 찾아 거기서 풀자" — 다음 카드가 이 지구의 가속을 통합한다.

---

## ▣ 크릴로프부분공간 / Krylov Subspace   지구15 · 선행: 지구01 고유값·SVD, ← 체비쇼프근사 → 잠금해제: #16, #22

### ① 의문
(체비쇼프 ⑤에서 받음.) 거대한 행렬 A(수백만×수백만)는 통째로 저장·계산이 불가능하다. 하지만 "A를 어떤 벡터에 곱하는 일(A·v)"은 빠르게 할 수 있는 경우가 많다(예: 대부분 0인 희소행렬). **A를 통째 안 보고, "A를 v에 거듭 곱한 흔적"만 모아 만든 *작은 공간* 안에서 거대한 문제(방정식 Ax=b, 고유값, exp(A)v)를 거의 정확히 풀 수 없을까?** (= "거대 최적화 가속"의 정점.)

### ② 벽
직접법(가우스 소거·역행렬)은 n×n 행렬에 *n³* 정도 계산이 들어, n이 백만이면 *우주 나이만큼* 걸린다. 행렬을 통째 메모리에 올리지도 못한다. 고유값을 다 구하는 것도 불가능하다. "A 전체"를 만지는 모든 방법이 *규모에서 무너진다*. 우리가 쓸 수 있는 건 오직 *A·v 곱하기* 한 가지뿐인데, 그것만으로 답에 어떻게 닿나?

```
   A 통째(백만×백만): 메모리·계산 불가  ✗
   가진 것:  v, Av, A²v, A³v, ... (A·곱하기만 반복) ← 이것만 가능
   크릴로프공간 Kₘ = span{v, Av, A²v, ..., A^{m-1}v}  (m≪n, 아주 작음)
   답 x*가 이 작은 공간 안에 거의 산다 → 작은 m차원 문제로 정사영해 풀기
```

### ③ 발명
벡터 v에 A를 거듭 곱한 것들이 펼치는 **크릴로프 부분공간 Kₘ = span{v, Av, A²v, …, A^{m−1}v}** 를 쓴다. m을 n보다 *훨씬 작게*(예: 백만 차원에 m=50) 잡아도, 큰 문제의 답이 이 작은 공간 안에 *거의 다 들어 있다*. 거기서 직교기저(아르놀디/란초스, Arnoldi/Lanczos)를 만들어 *작은 m×m 문제*로 줄여 푼다. 새 기호: 크릴로프 공간 **Kₘ**, span, 아르놀디 관계 **A Qₘ = Qₘ Hₘ + (나머지)**.

### ④ 해부

**R0 — 중2의 눈.**

   [중2 시각] 크릴로프 — 거대 행렬을 'A 곱하기' 흔적이 펼친 작은 방으로
```
   백만 차원 공간(거대)              작은 방 Kₘ (m=수십)
   ┌────────────────────┐          v ─A→ Av ─A→ A²v ─A→ A³v
   │  ·  · · · · · ·  ·  │          └───────┬───────────┘
   │ · 진짜 답 x* · · ·  │   ⇒        이 몇 개 화살표가 펼친 방
   │  · · ●x* · · · ·    │          ┌───────────┐
   │ · · · · · · · ·  ·  │          │  ●x* 거의  │  ← 답이 이 안에
   └────────────────────┘          │  여기 산다  │     거의 들어 있다
   백만 차원 직접 풀기 ✗            └───────────┘ 수십 차원만 풀면 끝
```
   거대한 A는 직접 못 보지만 "A 곱하기(A·v)"는 할 수 있다. v를 A로 거듭 변환한 화살표 몇 개가 펼친 작은 방 안에 진짜 답이 거의 다 들어 있다.
   We can't see giant A directly but can multiply A·v; a few repeated multiplications span a tiny room where the true answer nearly lives.

거대한 행렬 A는 직접 못 본다. 하지만 "A로 한 번 변환하기(A·v)"는 할 수 있다. 그래서 출발 벡터 v를 A로 한 번, 두 번, 세 번… 변환해 본다. 이렇게 나온 화살표 몇 개가 펼치는 *작은 방*(부분공간)이, 신기하게도 *진짜 답이 거의 사는 방*이다. 그 작은 방 안에서만 답을 찾으면, 백만 차원 문제가 *수십 차원* 문제로 쪼그라든다.

▲ **R1로 — 이유:** "작은 방"과 "그 안에서 푼다"를 식으로 적어야 한다.

**R1 — 고교의 손.**
$$K_m(A,\mathbf{v}) = \mathrm{span}\{\mathbf{v},\,A\mathbf{v},\,A^2\mathbf{v},\,\dots,\,A^{m-1}\mathbf{v}\}$$
한 글자씩:
- **Kₘ(A,v)**: m차원 크릴로프 부분공간. A와 출발벡터 v로 만든 "작은 방".
- **span{…}**: "이 화살표들을 더하고 늘여 만들 수 있는 모든 화살표의 집합"(이들이 펼치는 공간).
- **Aᵏv**: v에 A를 k번 곱한 화살표.
- **m ≪ n**: m은 n(원래 거대 차원)보다 *훨씬 작다*(≪는 "아주 많이 작다"). 백만 차원에 m=수십.

통째 의미: "A를 출발벡터에 거듭 곱한 몇 개 화살표가 펼치는 작은 공간. 큰 문제의 답이 이 안에 거의 다 있다." 왜? 케일리-해밀턴 정리상 A에 함수를 적용한 결과(A⁻¹b, exp(A)v 등)는 *A의 다항식*으로 표현되고, 다항식 p(A)v는 정확히 Kₘ 안에 들기 때문 — 체비쇼프(앞 카드)가 그 최적 다항식을 골라준다.

▲ **R2로 — 이유:** "작은 방을 어떻게 *정확히* 만들고, 거기서 어떻게 푸나"를 봐야 한다.

**R2 — 학부의 머리.**
v, Av, A²v…는 서로 거의 같은 방향으로 쏠려(수치적으로 위험) 직접 못 쓴다. **아르놀디 과정(Arnoldi)**(A가 대칭이면 더 싼 **란초스, Lanczos**)이 이들을 직교화해 깔끔한 기저 Qₘ을 만든다:
$$A Q_m = Q_m H_m + h_{m+1,m}\,\mathbf{q}_{m+1}\mathbf{e}_m^\top$$
- **Qₘ**: 크릴로프 공간의 *서로 직각인* 기저 화살표 m개를 모은 표(직교행렬).
- **Hₘ**: *작은* m×m 행렬(헤센베르크 꼴). A를 작은 방으로 *압축*한 것 — A의 "그림자".
- **나머지 항(hₘ₊₁,ₘ qₘ₊₁eₘᵀ)**: 작은 방 밖으로 *삐져나간* 부분. 이게 작아질수록 답이 방 안에 잘 갇힌 것.
- **eₘᵀ**: 마지막 칸만 1인 단위벡터(전치).

이제 큰 문제를 작은 Hₘ 문제로 *정사영(projection)*해 푼다 — Ax=b는 켤레기울기(CG, 대칭)·GMRES(일반)로, 고유값은 Hₘ의 고유값(리츠값, Ritz value)으로, exp(A)v는 Qₘ exp(Hₘ)(Qₘᵀv)로. 함정 못박기: 작은 방의 답이 *큰 답에 가깝다는 보장*은 A의 고유값 분포(스펙트럼)에 달렸다 — 고유값이 *뭉쳐 있으면 빠르게* 정확, *넓게 흩어지면 m을 키워도 느리다.* (함정: "작은 방으로 빠름"이 *항상* 정확한 게 아니라, *스펙트럼이 좋을 때만.* 빠름 ≠ 정답 보장.) 또 란초스는 반올림으로 직교성이 *무너져* 가짜 고유값(spurious)이 생긴다.

▲ **R3로 — 이유:** 이 지구 전체를 어떻게 묶는지, 무엇의 특수경우인지 봐야 한다.

**R3 — 대학원의 시야.**
크릴로프는 이 지구의 *가속 엔진을 통합*한다 — 신뢰영역의 스테이하우그-CG, 레벤버그-마카트의 JᵀJΔθ 풀이, 행렬지수 exp(A)v, 체비쇼프 다항식의 A 적용이 모두 크릴로프 위에서 돈다. 더 일반적으로 "A 전체 대신 A·v만으로 행렬함수 f(A)v를 푸는" 행렬-자유(matrix-free) 패러다임의 핵심이고, #16의 거대 선형계·#22의 대규모 동역학을 *현실적 시간*에 푼다. 무엇의 특수경우? 스펙트럼을 *작은 부분공간으로 압축*하는 일반 원리의 한 사례다.

▲ **R4로 — 이유:** "스펙트럼이 나쁠 때·비대칭일 때의 보장 부재"로.

**R4 — 최전선의 칼날.**
A의 고유값이 넓게 흩어지거나 비정규(non-normal)면 크릴로프 수렴이 *얼마나 느릴지*의 정확한 일반 예측, 그리고 그걸 고치는 *최적 전조건자(preconditioner)*를 자동으로 찾는 보편법은 — **여기서부터 인류가 다 모른다.** 전조건은 여전히 "문제마다의 기예"에 가깝고, 비대칭 큰 행렬의 크릴로프 안정성은 활발한 미해결 영역이다.

**【정밀층 — 집필표준 v2】**
[락] 본 카드에서 Kₘ은 크릴로프 부분공간, Qₘ은 직교기저, Hₘ은 압축 행렬(헤센베르크), λ는 (리츠값=근사) 고유값(감쇠인자 아님), v는 출발벡터.
R1° 정의역·공역: 크릴로프는 사상 (A, v, m) ↦ Kₘ = span{v, Av, …, A^{m−1}v} (m≪n), 그 위로 거대 문제(Ax=b, 고유값, exp(A)v)를 정사영해 작은 m×m로 푼다. 거대 행렬과 출발벡터를 작은 방으로 보낸다. 크릴로프(1931)·란초스·아르놀디가 정립.
R2° 보조정리: Lemma(아르놀디 관계) A Qₘ = Qₘ Hₘ + h_{m+1,m} q_{m+1}eₘᵀ. 귀납: q₁=v/‖v‖에서 시작해, Aqⱼ에서 이전 qᵢ들의 성분을 빼 직교화(그람-슈미트)하면 새 직교벡터 q_{j+1}이 나온다. 이를 행렬로 모으면 A를 Kₘ로 압축한 헤센베르크 Hₘ과 방 밖으로 삐져나간 나머지 항이 떨어진다 — 나머지가 작을수록 답이 방 안에 잘 갇힘.
R3° 구조 편입: 크릴로프 공간은 케일리-해밀턴(다항식 p(A)v가 Kₘ에 듦)에 근거하고, 이 지구의 가속 엔진(신뢰영역 스테이하우그-CG, LM의 JᵀJ 풀이, exp(A)v, 체비쇼프 p(A))을 행렬-자유 패러다임으로 통합. 대수↔해석 사상: A의 다항식 대수가 "부분공간 압축의 기하"를 "행렬함수 f(A)v의 해석"으로 잇는다.
R4° 파산(수식): 스펙트럼이 흩어지면 수렴 정체 + 직교성 붕괴 — 작은 방의 답이 큰 답에 가깝다는 보장은 A의 고유값 분포에 달려, 고유값이 넓게 흩어지거나 비정규(non-normal)면 수렴이 m을 키워도 느리고(스펙트럼 갭 부재), 란초스는 반올림으로 직교성 QₘᵀQₘ≈I가 무너져 가짜(spurious) 고유값이 생긴다. 최적 전조건자 자동 선택의 보편법 부재 → "작은 방으로 빠름"이 스펙트럼이 나쁘면 정확성을 잃는다.
쉽게: 거대 행렬은 직접 못 보지만 "A 곱하기"는 가능해, v를 거듭 곱한 화살표 몇 개가 펼친 작은 방에서 답을 찾는데, 고유값이 넓게 퍼져 있으면 그 방이 답을 잘 못 가둔다.

### ⑤ 새 의문
크릴로프는 거대한 행렬을 *작은 부분공간으로 압축*해 다룬다. 그런데 양자 다체계처럼 *수천 입자가 얽힌* 상태는, 표현하는 데 필요한 숫자가 2^수천 개로 — 행렬-벡터 곱조차 불가능할 만큼 폭발한다. 부분공간 압축의 정신을 *한 단계 더 밀어*, **수천 입자의 얽힘 구조 자체를 어떻게 작은 조각들의 그물로 압축해 다룰 수 없을까?** → 다음 파일 19, 지구16 **텐서네트워크·고차원**의 첫 의문으로. 닿는 난제: #16, #24.

**[도구 창조 씨앗]** 규칙 "*한* 출발벡터 v로 부분공간을 만든다"를 "*여러* 벡터(블록)로 동시에"로 뒤집으면? → 블록 크릴로프, 여러 우변·뭉친 고유값에 강함. 위험 검문소: **②정의** — 블록 안에서 벡터들이 *서로 종속*으로 무너지면(디플레이션 필요) "부분공간 차원"의 정의가 흔들려, 언제 어떤 벡터를 버릴지의 규칙이 모호해진다.

---

> 〔뿌리 줄기 — 다음 지구로 뻗는 큰 줄기〕 **이 지구15 한 그루를 다 키웠다.** "쪼개기와 가속하기" 한 뿌리에서 탐색 흩기(어닐링·PSO)·문제 나누기(ADMM) 줄기, 한 걸음 똑똑히(비선형최소제곱·레벤버그-마카트·신뢰영역·자동미분) 줄기, 거대 계산 압축(행렬지수·체비쇼프·크릴로프) 줄기가 뻗고, 마지막 크릴로프가 모든 가속 엔진을 *작은 부분공간 압축*이라는 한 원리로 통합했다.
> ```
> 지구15(계산최적화) ─── 한 그루 완성
>  └─ 다음 지구로 뻗는 큰 줄기: "크릴로프는 거대 행렬을 작은 부분공간으로 압축했다.
>        그런데 수천 입자가 얽힌 양자 상태는 숫자가 2^수천 개로 폭발해
>        행렬-벡터 곱조차 불가능하다. 압축의 정신을 한 단계 더 밀어,
>        *얽힘 구조 자체*를 작은 조각들의 그물로 압축할 수 없나?"
>        → 파일 19, 지구16(텐서네트워크·고차원)의 첫 도구로
> ```
> 부분공간 압축을 다 익혔으니 — 다음 지구는 그 압축을 *얽힘의 그물(텐서네트워크)*로 한 차원 더 밀어 올린다.

---

## 이 파일을 닫으며 (이음새 확인)

이 지구15는 "거대한 최적화를 어떻게 쪼개고 가속하나"라는 한 의문을 두 갈래로 공략했다 — **쪼개기**(어닐링·PSO로 탐색을 흩고, ADMM으로 문제를 나누고)와 **가속하기**(LM·신뢰영역으로 한 걸음을 똑똑히, 자동미분으로 미분을 정확히, 행렬지수·체비쇼프·크릴로프로 거대 계산을 압축). 매 카드에서 *함정* — "빠르다고 항상 정답에 가까워지진 않는다" — 을 못박았다: 어닐링의 실용적 식힘은 전역최소를 보장 못 하고, PSO는 조기수렴하고, 가우스-뉴턴은 폭주하고, ADMM은 수렴해도 느릴 수 있고, 크릴로프는 스펙트럼이 나쁘면 정확하지 않다. 마지막 크릴로프의 ⑤가 다음 파일 19(지구16, 텐서네트워크·고차원)의 첫 의문 — "수천 입자 얽힘을 어떻게 압축해 다루나" — 으로 다리를 놓았다.

---

(자가 감사 통과: 도구 10개 전부 ①②③④⑤·R0→R4 ▲표지·기호 첫등장 해부·중의성 못박음(λ 3뜻, ρ 2뜻, J 2뜻, Δ 2뜻, ε, x̄ 등)·공식 통째 해부·도구창조 씨앗+검문소·이음새 양끝·금지어 0·"여기서부터 인류가 모른다" 표기·ASCII 벽 삽입 완료.)

총 줄 수: 658줄.
